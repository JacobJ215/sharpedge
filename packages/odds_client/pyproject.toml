[project]
name = "sharpedge-odds"
version = "0.1.0"
description = "The Odds API client for SharpEdge"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "redis>=5.0",
    "pydantic>=2.0",
    "sharpedge-shared",
]

[tool.uv.sources]
sharpedge-shared = { workspace = true }
