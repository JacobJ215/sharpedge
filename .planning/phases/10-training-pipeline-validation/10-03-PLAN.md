---
phase: 10-training-pipeline-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/train_pm_models.py
  - tests/unit/scripts/test_train_pm_models.py
autonomous: true
requirements:
  - TRAIN-03
  - TRAIN-04

must_haves:
  truths:
    - "train_pm_models.py produces a .joblib artifact in data/models/pm/ for each category with >= 200 resolved markets and medium+ badge"
    - "Training report JSON contains calibration_score key (float or null) alongside quality_badge and market_count for every category entry"
    - "Categories below 200 markets are skipped with a WARNING entry but no artifact written"
    - "All test_train_pm_models.py tests pass including the new calibration_score test"
  artifacts:
    - path: "scripts/train_pm_models.py"
      provides: "Per-category RF training with calibration_score in report entries"
      contains: "calibration_score"
    - path: "tests/unit/scripts/test_train_pm_models.py"
      provides: "Green tests for TRAIN-03 and TRAIN-04"
      min_lines: 110
  key_links:
    - from: "scripts/train_pm_models.py"
      to: "data/models/pm/{category}.joblib"
      via: "joblib.dump(final_clf, model_path)"
      pattern: "joblib\\.dump"
    - from: "scripts/train_pm_models.py"
      to: "data/models/pm/training_report.json"
      via: "_write_entry() with calibration_score"
      pattern: "calibration_score"
---

<objective>
Add calibration_score to train_pm_models.py training report entries and verify the full training pipeline (artifact write + report JSON) works correctly.

Purpose: TRAIN-04 requires the report to contain calibration_score per category. TRAIN-03 requires .joblib artifacts to be written. The scripts exist from Phase 9 — this plan adds the missing calibration_score field and turns all formerly-xfail tests green.
Output: train_pm_models.py with calibration_score in report; all train tests passing.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-training-pipeline-validation/10-CONTEXT.md
@.planning/phases/10-training-pipeline-validation/10-RESEARCH.md
@.planning/phases/10-training-pipeline-validation/10-01-SUMMARY.md

<interfaces>
<!-- Key interfaces from the existing train_pm_models.py (already read above). -->

Current _write_entry call in train_category() (line 121, train_pm_models.py):
```python
_write_entry({"category": category, "skipped": False, "badge": badge, "market_count": len(df), "model_path": str(model_path)})
```
Required after change (add calibration_score):
```python
_write_entry({
    "category": category,
    "skipped": False,
    "badge": badge,
    "market_count": len(df),
    "calibration_score": float(brier_score_loss(oof_actuals, oof_probs)) if oof_probs and oof_actuals else None,
    "model_path": str(model_path),
})
```

OOF arrays already computed in train_category() (via _run_walk_forward):
```python
windows, oof_probs, oof_actuals = _run_walk_forward(X, y)
```
oof_probs: list[float], oof_actuals: list[int]

brier_score_loss import needed:
```python
from sklearn.metrics import brier_score_loss
```

Pitfall: brier_score_loss raises ValueError if oof_probs is empty or mismatched length.
Guard: `if oof_probs and oof_actuals and len(oof_probs) == len(oof_actuals)`

The skipped entry (insufficient data, line 88) does NOT need calibration_score.
The skipped entry (quality below minimum, line 105) should include calibration_score=None.

Current quality badge threshold (locked decision from CONTEXT.md):
- "low" badge AND len(windows) >= N_WINDOWS → skip, no artifact
- "medium"/"high"/"excellent" OR fewer windows than N_WINDOWS → write artifact
This logic already exists — no changes needed to the badge/gate logic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add calibration_score to train_pm_models.py report entries</name>
  <files>scripts/train_pm_models.py</files>
  <behavior>
    - Successful training entries contain calibration_score: float (Brier score on OOF data) or null if OOF unavailable
    - calibration_score is computed as brier_score_loss(oof_actuals, oof_probs) only when both lists are non-empty and same length
    - The low-badge skip entry (quality_below_minimum) receives calibration_score: None (key present, value null)
    - The insufficient-data skip entry does NOT need calibration_score (skipped before OOF is computed)
    - brier_score_loss is imported from sklearn.metrics
    - No other logic changes — badge gate, walk-forward, artifact write all unchanged
  </behavior>
  <action>
    Read scripts/train_pm_models.py before editing. Make two targeted edits:

    1. Add `from sklearn.metrics import brier_score_loss` to the imports at the top.

    2. In `train_category()`, compute calibration_score immediately before the final `_write_entry()` call:
       ```python
       calibration_score: float | None = None
       if oof_probs and oof_actuals and len(oof_probs) == len(oof_actuals):
           try:
               calibration_score = float(brier_score_loss(oof_actuals, oof_probs))
           except Exception as exc:
               logger.warning("calibration_score computation failed — %s", exc)
       ```

    3. Update the successful-training `_write_entry()` call (currently line 121) to include `"calibration_score": calibration_score`.

    4. Update the low-badge skip `_write_entry()` call (currently line 105) to include `"calibration_score": calibration_score` (value will be None at that point since OOF was computed but badge was low — or may have been skipped, in which case it stays None).

    Do NOT modify any other logic. Do NOT change the function signatures.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/scripts/test_train_pm_models.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_train_pm_models.py pass. The new test_train_category_report_includes_calibration_score test is green. Formerly-xfail tests (test_train_skips_category_below_200, test_train_produces_joblib_artifact, test_walk_forward_requires_3_windows) are also green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 10 training pipeline:
    - 006_resolved_pm_markets.sql migration (Plan 01)
    - download_pm_historical.py with Supabase upsert, preflight, volume filter (Plan 02)
    - process_pm_historical.py with Supabase SELECT (Plan 02)
    - train_pm_models.py with calibration_score in report (Plan 03)
    - Full test suite green
  </what-built>
  <how-to-verify>
    1. Run full test suite: `python -m pytest tests/unit/scripts/ -x -q`
       Expected: all tests pass, no xfail markers, no failures

    2. Verify migration file exists:
       `cat packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql`
       Expected: Contains UNIQUE(market_id, source) and ROW LEVEL SECURITY

    3. Verify calibration_score in train script:
       `grep -n "calibration_score" scripts/train_pm_models.py`
       Expected: Shows the brier_score_loss computation and _write_entry call with calibration_score

    4. (If you have live credentials) Run training pipeline end-to-end:
       ```
       python scripts/download_pm_historical.py
       python scripts/process_pm_historical.py
       python scripts/train_pm_models.py
       cat data/models/pm/training_report.json
       ```
       Expected: training_report.json contains badge, calibration_score, and market_count per trained category; .joblib files in data/models/pm/ for categories with sufficient data
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass and migration file is correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/scripts/ -x -q` — all tests green, no failures
2. `grep "calibration_score" scripts/train_pm_models.py` — shows brier_score_loss computation and _write_entry update
3. `python -m pytest tests/ -x -q` — full suite green (phase gate)
</verification>

<success_criteria>
- train_pm_models.py training report entries contain calibration_score (float or null)
- All test_train_pm_models.py tests pass, including the new calibration_score test
- Full pytest suite passes without failures
- Phase 10 human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/10-training-pipeline-validation/10-03-SUMMARY.md`
</output>
