---
phase: 10-training-pipeline-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql
  - tests/unit/scripts/test_download_pm_historical.py
  - tests/unit/scripts/test_process_pm_historical.py
  - tests/unit/scripts/test_train_pm_models.py
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-04

must_haves:
  truths:
    - "Running the full test suite passes with all new Wave 0 tests green (no xfail)"
    - "The resolved_pm_markets DDL file exists with UNIQUE(market_id, source) constraint and RLS policy"
    - "test_download_pm_historical.py contains a test that verifies Supabase upsert is called when SUPABASE_URL is set"
    - "test_download_pm_historical.py contains a test that verifies SystemExit when Kalshi credentials are present but auth fails"
    - "test_download_pm_historical.py contains a test that verifies Polymarket markets with volume <= 100 are excluded"
    - "test_process_pm_historical.py contains a test that verifies main() queries Supabase when SUPABASE_URL is set"
    - "test_train_pm_models.py contains a test that verifies report entry includes calibration_score key"
  artifacts:
    - path: "packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql"
      provides: "resolved_pm_markets DDL — required before any Supabase upsert in Plan 02 can work"
      contains: "UNIQUE(market_id, source)"
    - path: "tests/unit/scripts/test_download_pm_historical.py"
      provides: "New tests covering Supabase upsert, preflight exit, volume filter"
      min_lines: 260
    - path: "tests/unit/scripts/test_process_pm_historical.py"
      provides: "New test covering Supabase SELECT path"
      min_lines: 120
    - path: "tests/unit/scripts/test_train_pm_models.py"
      provides: "New test for calibration_score in report; existing xfail stubs become regular tests"
      min_lines: 110
  key_links:
    - from: "tests/unit/scripts/test_download_pm_historical.py"
      to: "scripts/download_pm_historical.py"
      via: "import backfill_kalshi_resolved, backfill_polymarket_resolved"
      pattern: "from scripts.download_pm_historical import"
    - from: "packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql"
      to: "Supabase resolved_pm_markets table"
      via: "migration runner"
      pattern: "CREATE TABLE IF NOT EXISTS resolved_pm_markets"
---

<objective>
Create the Supabase DDL migration for resolved_pm_markets and add all missing test coverage before implementation begins.

Purpose: Plans 02 and 03 implement against tests. Without the test scaffold and migration file, there is nothing to verify against. This plan locks the contracts first.
Output: Migration SQL file and extended test files with 6 new tests total.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-training-pipeline-validation/10-CONTEXT.md
@.planning/phases/10-training-pipeline-validation/10-RESEARCH.md

<interfaces>
<!-- Key patterns used in this plan. -->

From packages/database/src/sharpedge_db/migrations/003_prediction_markets.sql (existing precedent):
```sql
-- Pattern: UNIQUE constraint on (platform, market_id) prevents duplicate upserts
-- RLS enabled with service_role policy
ALTER TABLE pm_market_outcomes ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Service role full access" ON pm_market_outcomes FOR ALL TO service_role USING (true);
```

From packages/venue_adapters/src/sharpedge_venue_adapters/ledger.py (dual-mode pattern):
```python
url = os.environ.get("SUPABASE_URL")
key = os.environ.get("SUPABASE_SERVICE_ROLE_KEY")
if url and key:
    from supabase import create_client
    self._supabase = create_client(url, key)
```

Existing test file structure (tests/unit/scripts/test_download_pm_historical.py):
- Uses `patch("sharpedge_feeds.kalshi_client.KalshiClient.get_markets", new=AsyncMock(...))`
- Uses `patch.dict("os.environ", {"KALSHI_API_KEY": "fake-key"})`
- Uses `monkeypatch.delenv("KALSHI_API_KEY", raising=False)`

Existing xfail stubs in test_train_pm_models.py:
- test_train_skips_category_below_200 — xfail, will become GREEN
- test_train_produces_joblib_artifact — xfail, will become GREEN
- test_walk_forward_requires_3_windows — xfail, will become GREEN

Existing xfail stubs in test_process_pm_historical.py:
- test_process_kalshi_adds_universal_features — xfail, reads from parquet path
- test_process_polymarket_adds_universal_features — xfail, reads from parquet path
- test_low_data_category_filtered_count — xfail, reads from parquet path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create resolved_pm_markets migration DDL</name>
  <files>packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql</files>
  <behavior>
    - File must contain: CREATE TABLE IF NOT EXISTS resolved_pm_markets
    - Unique constraint: UNIQUE(market_id, source) — prevents Kalshi/Polymarket market_id collision
    - Canonical columns: id UUID PK, market_id TEXT NOT NULL, source TEXT NOT NULL ('kalshi' or 'polymarket'), title TEXT, category TEXT, market_prob DECIMAL, bid_ask_spread DECIMAL, last_price DECIMAL, volume DECIMAL, open_interest DECIMAL, days_to_close INTEGER, resolved_yes INTEGER NOT NULL, resolved_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()
    - Indexes: idx_resolved_pm_category on (category), idx_resolved_pm_source on (source)
    - RLS: ALTER TABLE resolved_pm_markets ENABLE ROW LEVEL SECURITY; CREATE POLICY "Service role full access to resolved_pm_markets" ON resolved_pm_markets FOR ALL TO service_role USING (true)
    - File naming follows existing convention: 006_resolved_pm_markets.sql (next after 005_clv_enhancements.sql)
  </behavior>
  <action>
    Create the SQL migration file. The file must define the resolved_pm_markets table that both Kalshi and Polymarket backfill rows upsert into. The UNIQUE(market_id, source) constraint is the idempotency key — Supabase upsert in download_pm_historical.py will use `on_conflict="market_id,source"`.

    Do NOT use a separate table per platform — one canonical table with a `source` column accommodates both. The `resolved_yes` column is INTEGER NOT NULL (0 or 1) to match both Kalshi result=="yes" normalization and Polymarket's native resolved_yes field.

    Follow the exact RLS pattern from 003_prediction_markets.sql and 004_calibration_tables.sql.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; sql = Path('packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql').read_text(); assert 'resolved_pm_markets' in sql; assert 'UNIQUE(market_id, source)' in sql; assert 'ROW LEVEL SECURITY' in sql; print('DDL OK')"</automated>
  </verify>
  <done>File exists, contains UNIQUE(market_id, source), RLS enabled, and CREATE TABLE IF NOT EXISTS resolved_pm_markets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Wave 0 test coverage for TRAIN-01, TRAIN-02, TRAIN-03, TRAIN-04</name>
  <files>tests/unit/scripts/test_download_pm_historical.py, tests/unit/scripts/test_process_pm_historical.py, tests/unit/scripts/test_train_pm_models.py</files>
  <behavior>
    test_download_pm_historical.py — add 3 new tests (append to existing file):
    - test_backfill_kalshi_upserts_to_supabase_when_url_set: patches `supabase.create_client` and `os.environ` with both SUPABASE_URL and KALSHI_API_KEY set, mocks KalshiClient.get_markets to return 1 market, asserts the mock Supabase client's table("resolved_pm_markets").upsert(...).execute() was called at least once
    - test_kalshi_preflight_exits_on_auth_failure: sets KALSHI_API_KEY in env, patches KalshiClient.get_markets to raise an Exception("401 Unauthorized"), calls backfill_kalshi_resolved with offline=False, asserts SystemExit is raised
    - test_polymarket_volume_filter_excludes_low_volume: mocks PolymarketClient.get_markets to return two markets (one volume=50.0, one volume=200.0), calls backfill_polymarket_resolved, asserts the resulting DataFrame contains only the high-volume market (len == 1)

    test_process_pm_historical.py — remove `@pytest.mark.xfail` from all 3 existing stubs (the parquet-based ones still work since process_kalshi/process_polymarket read from a provided path). Add 1 new test:
    - test_main_queries_supabase_when_url_set: patches `os.environ` with SUPABASE_URL set, patches `packages.database.src.sharpedge_db.client.get_supabase_client` (or the import path used in process_pm_historical.py) to return a mock Supabase client that returns an empty list from .select("*").execute(), asserts main() runs without error and the mock client's table("resolved_pm_markets").select was called

    test_train_pm_models.py — remove `@pytest.mark.xfail` from all 3 existing stubs. Add 1 new test:
    - test_train_category_report_includes_calibration_score: creates a 300-row DataFrame with balanced resolved_yes, calls train_category("political", df, model_dir=model_dir, report_path=report_path), opens the report JSON, asserts the political entry has key "calibration_score" (value may be None if OOF fails, but key must be present)
  </behavior>
  <action>
    IMPORTANT: Read each test file before editing. ALWAYS prefer editing the existing file over creating a new one.

    For test_download_pm_historical.py: append the 3 new test functions after the existing tests. The Supabase upsert test must patch `supabase.create_client` at the module level where download_pm_historical.py imports it (likely `scripts.download_pm_historical` module scope). Use `unittest.mock.patch` with `new_callable=MagicMock` and chain `.table().upsert().execute()` on the mock.

    For the preflight test: the test should NOT be in offline mode. It must set KALSHI_API_KEY and patch KalshiClient.get_markets to raise — then call the function and expect SystemExit. Note: backfill_kalshi_resolved currently does NOT have a preflight — this test will be RED until Plan 02 adds it. Write it as a regular test (not xfail) so it fails loudly until the implementation is in place.

    For test_process_pm_historical.py: remove `@pytest.mark.xfail` decorator from the 3 existing stubs. The parquet-path tests still pass since process_kalshi/process_polymarket accept a raw_path argument. Add the new Supabase test.

    For test_train_pm_models.py: remove `@pytest.mark.xfail` decorators. Add the calibration_score test. Note: the calibration_score test will be RED until Plan 03 adds the field — write as a regular test.

    After edits, run `python -m pytest tests/unit/scripts/ -x -q` and confirm:
    - Existing offline/fixture tests remain GREEN
    - New tests for Supabase upsert, preflight, volume filter are RED (implementation not yet done)
    - xfail removals turn the formerly-xfail tests GREEN (implementation already exists in Phase 9 scripts)
  </action>
  <verify>
    <automated>python -m pytest tests/unit/scripts/test_download_pm_historical.py tests/unit/scripts/test_process_pm_historical.py tests/unit/scripts/test_train_pm_models.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All formerly-xfail tests now run as regular tests. Existing passing tests remain green. New Wave 0 tests exist and are red (failing because implementation not yet done). No xfail markers remain in any of the three files.</done>
</task>

</tasks>

<verification>
1. `packages/database/src/sharpedge_db/migrations/006_resolved_pm_markets.sql` exists with UNIQUE(market_id, source) and RLS enabled
2. `tests/unit/scripts/test_download_pm_historical.py` has 3 new tests (Supabase upsert, preflight exit, volume filter)
3. `tests/unit/scripts/test_process_pm_historical.py` has xfail removed + 1 new Supabase test
4. `tests/unit/scripts/test_train_pm_models.py` has xfail removed + 1 new calibration_score test
5. Formerly-xfail tests are green (scripts already implemented in Phase 9)
6. New implementation-dependent tests are red (expected — implementations come in Plans 02/03)
</verification>

<success_criteria>
- Migration file 006_resolved_pm_markets.sql exists with full DDL, UNIQUE constraint, and RLS
- All three test files have been extended with new coverage
- No @pytest.mark.xfail decorators remain in the three test files
- Running `python -m pytest tests/unit/scripts/ -x -q` shows formerly-xfail tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/10-training-pipeline-validation/10-01-SUMMARY.md`
</output>
