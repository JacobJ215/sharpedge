---
phase: 08-frontend-polish-and-full-backend-wiring
plan: 07
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - tests/integration/test_rls_endpoints.py
  - packages/venue_adapters/tests/test_settlement_ledger_supabase.py
  - tests/unit/jobs/test_scanner_production_mode.py
autonomous: true
requirements:
  - WIRE-03

must_haves:
  truths:
    - "RLS JWT integration tests exist and are skipped in CI; they are ready to run against a real Supabase instance"
    - "LedgerStore Supabase mode tests exist and verify insert is called when env vars are present"
    - "Scanner production mode tests verify dry_run defaults correctly per PRODUCTION env var"
  artifacts:
    - path: "tests/integration/test_rls_endpoints.py"
      provides: "Real JWT RLS integration tests for 6 endpoints with cross-user isolation"
      min_lines: 60
    - path: "packages/venue_adapters/tests/test_settlement_ledger_supabase.py"
      provides: "LedgerStore Supabase mode verification"
      min_lines: 35
    - path: "tests/unit/jobs/test_scanner_production_mode.py"
      provides: "Scanner dry_run production mode verification"
      min_lines: 25
  key_links:
    - from: "tests/integration/test_rls_endpoints.py"
      to: "apps/webhook_server/src/sharpedge_webhooks/main.py"
      via: "TestClient(app) against registered routes"
      pattern: "TestClient.*app"
    - from: "packages/venue_adapters/tests/test_settlement_ledger_supabase.py"
      to: "packages/venue_adapters/src/sharpedge_venue_adapters/settlement_ledger.py"
      via: "import LedgerStore"
      pattern: "from.*settlement_ledger import LedgerStore"
---

<objective>
Implement WIRE-03 verification tests: real JWT RLS integration tests, LedgerStore Supabase mode tests, and scanner production mode tests. All three replace Wave 0 stubs with real implementations.

Purpose: Verify that RLS enforcement, the LedgerStore Supabase write path, and scanner dry_run logic work correctly without requiring code changes to production modules.
Output: Three test files — RLS integration (7 tests, SKIPPED in CI), LedgerStore Supabase (2 tests, SKIPPED in CI), scanner production mode (2 tests, GREEN in CI).
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-CONTEXT.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-RESEARCH.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly — no exploration needed. -->

From apps/webhook_server/tests/unit/api/test_rls.py (existing pattern):
```python
import pytest
from fastapi.testclient import TestClient
from sharpedge_webhooks.main import app
client = TestClient(app)
```

From packages/venue_adapters/tests/test_snapshot_store.py (existing pattern):
```python
from sharpedge_venue_adapters.snapshot_store import SnapshotStore
# Tests use in-memory mode (no env vars). Supabase mode tests need SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY.
```

From apps/bot/src/sharpedge_bot/jobs/value_scanner_job.py:
```python
# send_fcm_notifications_for_play(play) called at line 336
# _pending_value_alerts.append(play) called at line 337
# dry_run flag controls whether scanner skips writes; inspect actual module for mechanism
```

Wave 0 stubs (in 08-01 files_modified) — read these before replacing:
- tests/integration/test_rls_endpoints.py (NotImplementedError stubs, skipif on SUPABASE_URL)
- packages/venue_adapters/tests/test_settlement_ledger_supabase.py (NotImplementedError stubs, skipif on SUPABASE_URL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real JWT RLS integration tests (replaces Wave 0 stubs)</name>
  <files>
    tests/integration/test_rls_endpoints.py
  </files>
  <behavior>
    Replace Wave 0 NotImplementedError stubs with real JWT integration tests:

    pytestmark = pytest.mark.skipif(not os.getenv("SUPABASE_URL"), reason="integration test — requires SUPABASE_URL")

    Fixture: `user_a_jwt` — calls supabase.auth.sign_in_with_password({"email": os.environ["TEST_USER_A_EMAIL"], "password": os.environ["TEST_USER_A_PASSWORD"]}) and returns session.access_token

    Fixture: `user_b_jwt` — same pattern with TEST_USER_B_EMAIL / TEST_USER_B_PASSWORD

    6 test functions (all real, no NotImplementedError):
    - test_value_plays_requires_jwt: GET /api/v1/value-plays with no Authorization → 401; with user_a_jwt → 200
    - test_game_analysis_requires_jwt: GET /api/v1/game-analysis with no Authorization → 401; with user_a_jwt → 200 or 422
    - test_copilot_requires_jwt: POST /api/v1/copilot/chat with no Authorization → 200 (copilot is public but threads user_id when present)
    - test_portfolio_requires_jwt: GET /api/v1/portfolio with no Authorization → 401; with user_a_jwt → 200
    - test_bankroll_simulate_requires_jwt: POST /api/v1/bankroll/simulate with no Authorization → 200 (public endpoint); verify response schema
    - test_dislocation_requires_jwt: GET /api/v1/markets/dislocation?market_id=TEST → 200 with empty scores (public endpoint)

    Cross-user isolation test:
    - test_portfolio_cross_user_isolation: user_a_jwt fetches portfolio → records portfolio_ids; user_b_jwt fetches portfolio → asserts no overlap with user_a portfolio_ids (RLS enforcement)
  </behavior>
  <action>
    Read the existing tests/integration/test_rls_endpoints.py (Wave 0 stub). Replace the NotImplementedError stubs with real test implementations.

    Use `from fastapi.testclient import TestClient` for unit-mode tests and the actual supabase-py client for fixture JWT generation. Import: `from supabase import create_client; supabase_client = create_client(os.environ["SUPABASE_URL"], os.environ["SUPABASE_ANON_KEY"])`.

    For JWT fixture: call `supabase_client.auth.sign_in_with_password({"email": ..., "password": ...})` and extract `res.session.access_token`. This requires TEST_USER_A_EMAIL, TEST_USER_A_PASSWORD, TEST_USER_B_EMAIL, TEST_USER_B_PASSWORD env vars — document in the test file header comment.

    Keep the skipif guard so the entire module is skipped when SUPABASE_URL is absent (CI safety).
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && python -m pytest tests/integration/test_rls_endpoints.py -v --collect-only 2>&1 | tail -20</automated>
  </verify>
  <done>
    `--collect-only` shows 7 test items (6 endpoint tests + 1 cross-user isolation test), all with skipif markers.
    Running without SUPABASE_URL: all 7 tests show SKIPPED.
    No NotImplementedError stubs remain in the file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LedgerStore Supabase mode verification (replaces Wave 0 stub)</name>
  <files>
    packages/venue_adapters/tests/test_settlement_ledger_supabase.py
  </files>
  <behavior>
    Replace Wave 0 NotImplementedError stubs with real LedgerStore Supabase mode tests:

    pytestmark = pytest.mark.skipif(not os.getenv("SUPABASE_URL"), reason="integration test — requires SUPABASE_URL")

    test_ledger_store_supabase_mode_not_none:
    - Set SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in environment (monkeypatch or assume present when skipif passes)
    - Instantiate LedgerStore
    - Assert `store._supabase is not None` (pattern mirrors test_snapshot_store.py Supabase mode)

    test_ledger_store_record_calls_insert:
    - Mock `supabase.table("ledger_entries").insert()` via unittest.mock.patch
    - Call LedgerStore.record(entry_dict)
    - Assert insert mock was called once with the entry dict
    - This verifies the Supabase write path without hitting a real DB
  </behavior>
  <action>
    Read the existing packages/venue_adapters/tests/test_settlement_ledger_supabase.py (Wave 0 stub). Replace NotImplementedError stubs.

    Import `from sharpedge_venue_adapters.settlement_ledger import LedgerStore` (adjust module path — check actual package structure by looking at packages/venue_adapters/src/). Use unittest.mock.patch for the insert call mock.

    Keep skipif guard at module level. File should stay under 60 lines.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && python -m pytest packages/venue_adapters/tests/test_settlement_ledger_supabase.py -v --collect-only 2>&1 | tail -15</automated>
  </verify>
  <done>
    `--collect-only` shows 2 test items with skipif markers.
    Running without SUPABASE_URL: both tests show SKIPPED.
    No NotImplementedError stubs remain.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scanner production mode test</name>
  <files>
    tests/unit/jobs/test_scanner_production_mode.py
  </files>
  <behavior>
    test_scanner_dry_run_defaults_false_in_production:
    - With PRODUCTION=1 env var set (monkeypatch): assert value_scanner_job dry_run default is False
    - OR: directly inspect value_scanner_job module-level variable / function signature default
    - Assert the scanner does not skip writes when PRODUCTION=1 (dry_run is False)

    test_scanner_dry_run_true_without_production_env:
    - Without PRODUCTION env var: assert dry_run defaults to True (safe default in dev)
  </behavior>
  <action>
    Create tests/unit/jobs/test_scanner_production_mode.py. Import value_scanner_job and inspect the dry_run parameter default or module-level flag. Use monkeypatch.setenv("PRODUCTION", "1") to test the production branch.

    If value_scanner_job has no explicit dry_run parameter, read the file to find the actual production flag mechanism and write the test accordingly.

    Keep file under 50 lines.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run pytest tests/unit/jobs/test_scanner_production_mode.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    Both scanner production mode tests PASS.
    No production code modified.
  </done>
</task>

</tasks>

<verification>
```
uv run pytest tests/integration/test_rls_endpoints.py packages/venue_adapters/tests/test_settlement_ledger_supabase.py packages/venue_adapters/tests/test_snapshot_store_supabase.py -v --collect-only
uv run pytest tests/unit/jobs/test_scanner_production_mode.py -v
```
First command: 7 + 2 + existing Supabase tests all collected, all with skipif markers, SKIPPED when SUPABASE_URL absent.
Second command: 2 scanner tests PASS.
</verification>

<success_criteria>
- RLS integration tests: 7 tests collected, all SKIPPED without SUPABASE_URL, ready to run against real Supabase
- LedgerStore Supabase tests: 2 tests collected, SKIPPED without SUPABASE_URL
- Scanner production mode tests: 2 tests GREEN in CI
- No NotImplementedError stubs remain in any of the three files
- No production code modified
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-polish-and-full-backend-wiring/08-07-SUMMARY.md`
</output>
