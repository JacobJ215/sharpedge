---
phase: 08-frontend-polish-and-full-backend-wiring
plan: 06
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
files_modified:
  - apps/web/src/app/(dashboard)/copilot/page.tsx
  - apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py
  - apps/mobile/lib/screens/copilot_screen.dart
  - packages/agent_pipeline/tests/test_copilot_tools_count.py
  - packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py
autonomous: false
requirements:
  - WIRE-06

must_haves:
  truths:
    - "All 12 BettingCopilot tools are reachable and return non-error responses from the web chat UI"
    - "BettingCopilot chat on web passes the user's auth token so user-context tools (get_active_bets, get_portfolio_stats) return the correct user's data"
    - "BettingCopilot chat on Flutter (CopilotScreen) passes the auth token via SSE POST headers"
    - "A 10+ turn conversation stays within the context window (trim_conversation is active)"
    - "Streaming SSE tokens appear in real time in the web UI (not batched)"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/copilot/page.tsx"
      provides: "Copilot page updated to pass auth token in SSE chat request"
      min_lines: 50
    - path: "apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py"
      provides: "Copilot endpoint with optional Authorization header threading user_id into tool state"
      min_lines: 50
    - path: "apps/mobile/lib/screens/copilot_screen.dart"
      provides: "CopilotScreen updated to pass auth token in SSE POST headers"
      min_lines: 80
    - path: "packages/agent_pipeline/tests/test_copilot_tools_count.py"
      provides: "GREEN test asserting len(COPILOT_TOOLS) == 12"
      min_lines: 20
    - path: "packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py"
      provides: "get_active_bets and get_portfolio_stats read user_id from LangGraph configurable state"
      min_lines: 80
  key_links:
    - from: "apps/web/src/app/(dashboard)/copilot/page.tsx"
      to: "apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py"
      via: "fetch POST /api/v1/copilot/chat with Authorization: Bearer {token}"
      pattern: "Authorization.*Bearer"
    - from: "apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py"
      to: "packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py"
      via: "COPILOT_TOOLS = 12 tools injected into graph state with user_id"
      pattern: "user_id.*COPILOT_TOOLS"
    - from: "apps/mobile/lib/screens/copilot_screen.dart"
      to: "apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py"
      via: "http.Request POST with Authorization header"
      pattern: "'Authorization'.*token"
    - from: "apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py"
      to: "packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py"
      via: "graph config injects user_id; get_active_bets/get_portfolio_stats reads config['configurable']['user_id']"
      pattern: "configurable.*user_id"
---

<objective>
Complete WIRE-06 — all 12 BettingCopilot tools exercised end-to-end from both web and mobile surfaces. The copilot endpoint needs an optional auth header to thread user context into user-context tools. The web and mobile clients must pass the auth token.

Purpose: User-context tools (get_active_bets, get_portfolio_stats) currently return generic/demo data because no user JWT is threaded through. This plan wires auth token from both clients so copilot tools return the correct user's data.
Output: Auth-aware copilot endpoint, updated web and Flutter clients, GREEN tool count test, human-verified e2e journey.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-CONTEXT.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-RESEARCH.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-02-SUMMARY.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. -->

From apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py (existing):
```python
# Currently: POST /api/v1/copilot/chat — public endpoint, no auth
# Must add: optional Authorization header; if present, extract user_id and pass to graph state
# Pattern: use Optional[str] = Header(None) for the auth header (not required)
```

From apps/webhook_server/src/sharpedge_webhooks/routes/v1/deps.py:
```python
# get_current_user raises 401 if token invalid — too strict for copilot (should degrade, not block)
# For copilot: use optional auth — if header present, validate; if absent, continue with user_id=None
```

From apps/web/src/app/(dashboard)/copilot/page.tsx (existing — likely uses chat-stream pattern):
```typescript
// Existing SSE POST pattern from chat-stream.tsx:
// fetch('/api/v1/copilot/chat', { method: 'POST', body: JSON.stringify({message}), headers: {'Content-Type': 'application/json'} })
// Add: 'Authorization': `Bearer ${token}` where token = supabase.auth.getSession().access_token
```

From apps/mobile/lib/screens/copilot_screen.dart (existing SSE pattern):
```dart
// Existing: http.Request with streaming — already handles SSE
// Add: request.headers['Authorization'] = 'Bearer ${widget.authToken}';
// or: read from AppState.authToken via Provider
```

From packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py:
```python
COPILOT_TOOLS = [
    get_active_bets,        # needs user_id from config['configurable']['user_id']
    get_portfolio_stats,    # needs user_id from config['configurable']['user_id']
    analyze_game,           # public
    search_value_plays,     # public
    check_line_movement,    # public
    get_sharp_indicators,   # public
    estimate_bankroll_risk, # public
    get_prediction_market_edge,  # public
    compare_books,          # public
    get_model_predictions,  # public
] + VENUE_TOOLS  # get_venue_dislocation, get_exposure_status = 12 total
```

Tool invocation: each tool is a LangChain StructuredTool. User-context tools (get_active_bets, get_portfolio_stats) check for user_id in their input schema or tool state. Review the tool implementations to understand how to inject user_id — most likely via a config key in the graph state, not tool input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth-aware copilot endpoint + web client token forwarding</name>
  <files>
    apps/webhook_server/src/sharpedge_webhooks/routes/v1/copilot.py,
    apps/web/src/app/(dashboard)/copilot/page.tsx,
    packages/agent_pipeline/tests/test_copilot_tools_count.py,
    packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py
  </files>
  <behavior>
    copilot.py (modify existing):
    - Add optional Authorization header: `authorization: Optional[str] = Header(None, alias="Authorization")`
    - If authorization present: extract token with `token = authorization.removeprefix("Bearer ").strip()`
    - Use get_current_user logic (inline, not via Depends) to resolve user_id from token
    - If token invalid: continue with user_id = None (degrade gracefully — do not return 401)
    - Pass user_id into the graph config: `config={"configurable": {"user_id": user_id}}`
    - Tools that need user_id check config['configurable']['user_id']; if None, they return placeholder data (existing behavior unchanged for unauthenticated calls)

    tools.py (modify get_active_bets and get_portfolio_stats):
    - Update get_active_bets to accept and use `config: RunnableConfig` parameter (LangChain pattern)
    - Read `user_id = config["configurable"].get("user_id")` inside the tool function
    - If user_id is None: return placeholder/demo data (backward compat)
    - If user_id is set: query real user data using user_id
    - Apply same pattern to get_portfolio_stats
    - Do NOT change public tools (analyze_game, search_value_plays, etc.)
    - COPILOT_TOOLS list remains unchanged (still 12 entries)

    copilot/page.tsx (modify existing):
    - Read supabase.auth.getSession() to get access_token
    - Add 'Authorization': `Bearer ${token}` to the fetch headers in the SSE POST call
    - Keep all existing streaming/rendering logic unchanged

    test_copilot_tools_count.py (from Wave 0 — this should already be GREEN):
    - Verify len(COPILOT_TOOLS) == 12
    - If Wave 0 created this as a GREEN test, just confirm it passes; no changes needed
    - If Wave 0 left it as a RED stub, implement: `from sharpedge_agent_pipeline.copilot.tools import COPILOT_TOOLS; assert len(COPILOT_TOOLS) == 12`
  </behavior>
  <action>
    Read copilot.py first. The endpoint currently streams SSE — the auth header extraction must happen before the streaming generator starts. Add `from typing import Optional` and `from fastapi import Header` imports if not present. Extract user_id before the `async def generate()` function is defined, so it's captured in the closure.

    Read tools.py first. Update get_active_bets and get_portfolio_stats: add `config: RunnableConfig = None` parameter (or use LangChain's `@tool` config injection pattern). Read config['configurable'].get('user_id', None) inside the function. If user_id is present, query real data; if None, return demo data. This is the standard LangChain pattern for injecting request-scoped context into tools.

    Read copilot/page.tsx. The SSE fetch is likely in a useCallback or useEffect. Add `const { data: { session } } = await supabase.auth.getSession()` before the fetch call. Add Authorization header.

    Keep the copilot endpoint backward-compatible: no Authorization header → user_id = None → tools return demo data (existing behavior). Adding header → tools return real user data.

    Using X per user decision: tools.py is modified (added to files_modified) to wire the user_id configurable — this is the correct integration point rather than passing user_id through tool input schemas.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run pytest packages/agent_pipeline/tests/test_copilot_tools_count.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_copilot_tools_count.py PASSES: len(COPILOT_TOOLS) == 12.
    copilot.py accepts optional Authorization header (TestClient confirms: header present → user_id extracted; absent → user_id None, no 401).
    copilot/page.tsx sends Authorization header in SSE POST fetch.
    tools.py: get_active_bets and get_portfolio_stats read user_id from config['configurable']['user_id'].
    `npm run build` succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flutter CopilotScreen auth token forwarding</name>
  <files>
    apps/mobile/lib/screens/copilot_screen.dart
  </files>
  <behavior>
    CopilotScreen (modify existing):
    - Read AppState.authToken (via Provider.of<AppState>) or accept authToken as constructor param
    - Add 'Authorization': 'Bearer $authToken' to the http.Request headers before the stream is initiated
    - If authToken is null or empty: send request without auth header (public copilot still works)
    - Keep all existing SSE streaming, message list rendering, and input field logic unchanged
    - Keep file under 500 lines
  </behavior>
  <action>
    Read copilot_screen.dart first. Find the http.Request construction and the `.send()` call. Add the Authorization header before send(). Use `Provider.of<AppState>(context, listen: false).authToken` to get the token — AppState already exposes `String? get authToken`.

    If CopilotScreen takes no constructor params currently: use Provider pattern, not constructor injection (less refactoring).

    Do not change the SSE parsing logic, the message list, or the text input behavior — only add one header to the outgoing request.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge/apps/mobile && flutter analyze lib/screens/copilot_screen.dart 2>&1 | tail -10</automated>
  </verify>
  <done>
    `flutter analyze lib/screens/copilot_screen.dart` reports no errors.
    Auth token is present in the SSE POST request headers (verified by code inspection).
    copilot_screen.dart is under 500 lines.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>BettingCopilot full e2e — auth token wired from web and mobile, all 12 tools reachable.</what-built>
  <how-to-verify>
    Web e2e journey (full path):
    1. Open http://localhost:3000 (or production URL) — verify redirected to /auth/login if not logged in
    2. Login with email + password — verify redirected to dashboard
    3. Navigate to /plays — verify value plays load with alpha badges
    4. Navigate to /markets — verify venue dislocation widget shows data
    5. Navigate to /bankroll — verify exposure widget shows data
    6. Navigate to /copilot — open chat
    7. Send these messages in order (10+ turns):
       - "What are my active bets?"
       - "What's my portfolio ROI?"
       - "Analyze game [any game ID from value plays]"
       - "What's the line movement on [sport]?"
       - "What are the sharp indicators?"
       - "What's my bankroll risk if I bet $500?"
       - "Show me prediction market edges"
       - "Compare books for [event]"
       - "What do the models predict for tonight?"
       - "What's the venue dislocation on KXBTCD-01?"
       - "What's my Kelly exposure status?"
    8. Verify: all 10+ messages get streaming responses (tokens appear one by one)
    9. Verify: no "context limit reached" or truncation error after turn 10

    Flutter e2e (on device):
    1. Open app, login with biometrics
    2. Navigate to Copilot tab
    3. Send 3+ messages, verify SSE streaming in real time
    4. Verify: "What are my active bets?" returns real user data (not demo data)

    Acceptable: some tools return empty data if backend has no live data. Unacceptable: error messages, 500 responses, missing streaming tokens, or context window exceeded after 10 turns.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 tools respond from web and SSE verified from Flutter, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Automated:
```
cd /Users/revph/sharpedge && uv run pytest packages/agent_pipeline/tests/test_copilot_tools_count.py -v
cd /Users/revph/sharpedge/apps/mobile && flutter analyze lib/screens/copilot_screen.dart
cd /Users/revph/sharpedge/apps/web && npm run build
```

Manual checkpoint: Full e2e journey verified by human — web 10-turn conversation + Flutter SSE streaming.
</verification>

<success_criteria>
- test_copilot_tools_count.py: len(COPILOT_TOOLS) == 12 GREEN
- copilot.py accepts optional Authorization header without returning 401
- copilot.py passes user_id into graph config['configurable']['user_id']
- tools.py: get_active_bets and get_portfolio_stats read user_id from configurable state
- copilot/page.tsx sends auth token in SSE POST headers
- copilot_screen.dart sends auth token in SSE POST headers
- Human approved full e2e journey: web 10+ turns + Flutter streaming verified
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-polish-and-full-backend-wiring/08-06-SUMMARY.md`
</output>
