---
phase: 08-frontend-polish-and-full-backend-wiring
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - apps/web/src/components/venue/VenueDislocWidget.tsx
  - apps/web/src/components/venue/ExposureWidget.tsx
  - apps/web/src/app/(dashboard)/prediction-markets/page.tsx
  - apps/web/src/app/(dashboard)/bankroll/page.tsx
autonomous: true
requirements:
  - WIRE-02

must_haves:
  truths:
    - "The /markets page shows a VenueDislocWidget section with venue-by-venue dislocation scores from the real GET /api/v1/markets/dislocation endpoint"
    - "The /bankroll page shows an ExposureWidget section with Kelly utilization bars from the real GET /api/v1/bankroll/exposure endpoint"
    - "Both widgets show a loading skeleton while fetching and an error state if the endpoint is unavailable"
    - "Both widgets display real data shapes — not hardcoded mock values"
  artifacts:
    - path: "apps/web/src/components/venue/VenueDislocWidget.tsx"
      provides: "Venue dislocation table component — renders consensus_prob + per-venue disloc_bps rows"
      min_lines: 60
    - path: "apps/web/src/components/venue/ExposureWidget.tsx"
      provides: "Exposure/Kelly utilization widget — renders utilization bars per venue"
      min_lines: 55
    - path: "apps/web/src/app/(dashboard)/prediction-markets/page.tsx"
      provides: "Prediction markets page extended with VenueDislocWidget"
      min_lines: 50
    - path: "apps/web/src/app/(dashboard)/bankroll/page.tsx"
      provides: "Bankroll page extended with ExposureWidget"
      min_lines: 50
  key_links:
    - from: "apps/web/src/app/(dashboard)/prediction-markets/page.tsx"
      to: "apps/web/src/components/venue/VenueDislocWidget.tsx"
      via: "import VenueDislocWidget"
      pattern: "VenueDislocWidget"
    - from: "apps/web/src/components/venue/VenueDislocWidget.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "fetch('/api/v1/markets/dislocation')"
      pattern: "api/v1/markets/dislocation"
    - from: "apps/web/src/components/venue/ExposureWidget.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "fetch('/api/v1/bankroll/exposure')"
      pattern: "api/v1/bankroll/exposure"
---

<objective>
Build the Phase 6 UI widgets for the web dashboard. VenueDislocWidget goes on the prediction markets page; ExposureWidget goes on the bankroll page. Both fetch from the real FastAPI endpoints created in Wave 1.

Purpose: Complete WIRE-02 — surface Phase 6 venue intelligence in the user-visible UI for the first time.
Output: Two new React components + updated prediction-markets and bankroll pages.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-CONTEXT.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-RESEARCH.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. Executor uses these directly. -->

API response shapes (from Wave 1 endpoints, confirmed in test_dislocation_endpoint.py):
```typescript
// GET /api/v1/markets/dislocation?market_id=X
interface DislocResponse {
  consensus_prob: number
  scores: Array<{
    venue_id: string
    mid_prob: number
    disloc_bps: number  // basis points; positive = venue priced higher than consensus
    is_stale: boolean
  }>
}

// GET /api/v1/bankroll/exposure
interface ExposureResponse {
  total_exposure: number
  bankroll: number
  venues: Array<{
    venue_id: string
    exposure: number
    utilization_pct: number  // 0-100
  }>
}
```

From apps/web/src/lib/api.ts (existing — all v1 endpoints use Authorization: Bearer ${token}):
```typescript
// Pattern for new fetches: pass token from supabase.auth.getSession()
const { data: { session } } = await supabase.auth.getSession()
const token = session?.access_token ?? ''
// Dislocation endpoint is public — no auth header needed
const res = await fetch(`/api/v1/markets/dislocation?market_id=${marketId}`)
```

Existing page patterns (read before modifying):
- apps/web/src/app/(dashboard)/bankroll/page.tsx — has Monte Carlo chart; extend, do not replace
- apps/web/src/app/(dashboard)/prediction-markets/page.tsx — has PM edge table; extend, do not replace

Style conventions from existing components:
```typescript
// Zinc palette, compact trading-terminal aesthetic
// Table rows: border-zinc-800, text-zinc-300, text-xs
// Section heading: text-xs font-semibold uppercase tracking-widest text-zinc-500
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VenueDislocWidget component + prediction markets page wiring</name>
  <files>
    apps/web/src/components/venue/VenueDislocWidget.tsx,
    apps/web/src/app/(dashboard)/prediction-markets/page.tsx
  </files>
  <behavior>
    VenueDislocWidget:
    - Props: { marketId: string } — fetches from /api/v1/markets/dislocation?market_id={marketId} on mount
    - Loading state: renders skeleton div while fetching
    - Error state: renders "Dislocation data unavailable" in text-zinc-500 when fetch fails
    - Success state: renders a table with columns: Venue | Mid Prob | Disloc (bps) | Stale
    - Shows consensus_prob as a header stat: "Consensus: {(consensus_prob * 100).toFixed(1)}%"
    - Disloc column: color red for negative bps (underpriced at this venue), green for positive, zinc for ~0
    - Stale venues: show "(stale)" suffix in text-zinc-600
    - Fetches without auth token (public endpoint)

    prediction-markets page:
    - Read existing page first — preserve all existing PM edge table content
    - Add VenueDislocWidget at bottom of page, wrapped in a section with heading "Cross-Venue Dislocation"
    - Default marketId prop: use first market_id from the value plays list if available, or a static example "KXBTCD-01" as placeholder
  </behavior>
  <action>
    Create apps/web/src/components/venue/ directory. Create VenueDislocWidget.tsx as a 'use client' component. Use React useState + useEffect for data fetching. No SWR or React Query — use plain fetch() consistent with existing page patterns in this codebase.

    Read prediction-markets/page.tsx first, then add the import and widget at the bottom of the rendered JSX. Do not remove or replace existing content.

    Component must handle: null marketId (render nothing), empty scores array (render "No dislocation data for this market"), and network errors (graceful error message).
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge/apps/web && npm test -- --run src/test/venue-dislocation.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>
    venue-dislocation.test.tsx PASSES — VenueDislocWidget renders given the DislocResponse shape.
    `npm run build` succeeds with no TypeScript errors.
    prediction-markets page.tsx imports VenueDislocWidget.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExposureWidget component + bankroll page wiring</name>
  <files>
    apps/web/src/components/venue/ExposureWidget.tsx,
    apps/web/src/app/(dashboard)/bankroll/page.tsx
  </files>
  <behavior>
    ExposureWidget:
    - No props — fetches /api/v1/bankroll/exposure on mount
    - Loading state: skeleton div
    - Error state: "Exposure data unavailable" in text-zinc-500
    - Success state:
      - Header: "Total Exposure: ${total_exposure.toFixed(0)} / Bankroll: ${bankroll.toFixed(0)}"
      - Per-venue row: venue_id | $exposure | utilization_pct% progress bar
      - Progress bar: div with width={utilization_pct}%, bg-amber-500 for >50%, bg-green-600 for ≤50%
      - Overall utilization row: sum of venue exposures / bankroll as a total bar
    - If venues array is empty: "No active positions"
    - Fetches without auth token (public endpoint — in-memory ExposureBook)

    bankroll page:
    - Read existing bankroll/page.tsx first — preserve Monte Carlo fan chart and Kelly calculator
    - Add ExposureWidget below the Monte Carlo chart, wrapped in section with heading "Live Exposure (Current Session)"
    - Add a note below widget: "(Resets on server restart — reflects current process positions)"
  </behavior>
  <action>
    Create ExposureWidget.tsx as 'use client'. Use same fetch pattern as VenueDislocWidget. Keep file under 200 lines.

    Read bankroll/page.tsx first. Add import at top, add ExposureWidget in JSX below the chart section. Do not modify existing chart or calculator logic.

    ExposureWidget progress bar: `<div style={{ width: \`${Math.min(utilization_pct, 100)}%\` }} className="h-1.5 rounded ..." />`
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge/apps/web && npm test -- --run src/test/exposure-widget.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>
    exposure-widget.test.tsx PASSES — ExposureWidget renders utilization bars with ExposureResponse shape.
    `npm run build` succeeds.
    bankroll page.tsx imports and renders ExposureWidget.
    Both test files (venue-dislocation + exposure-widget) are GREEN.
  </done>
</task>

</tasks>

<verification>
Full WIRE-02 check:
```
cd /Users/revph/sharpedge/apps/web && npm test -- --run src/test/venue-dislocation.test.tsx src/test/exposure-widget.test.tsx
```
Must exit 0. Both widget component files exist with correct import structure.
</verification>

<success_criteria>
- VenueDislocWidget.tsx exists in apps/web/src/components/venue/ with loading/error/success states
- ExposureWidget.tsx exists in same directory with utilization progress bars
- prediction-markets/page.tsx renders VenueDislocWidget
- bankroll/page.tsx renders ExposureWidget
- Both widget tests GREEN
- `npm run build` in apps/web exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-polish-and-full-backend-wiring/08-03-SUMMARY.md`
</output>
