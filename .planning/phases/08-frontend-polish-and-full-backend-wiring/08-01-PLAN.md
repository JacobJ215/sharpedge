---
phase: 08-frontend-polish-and-full-backend-wiring
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - apps/web/src/test/auth.test.tsx
  - apps/web/src/test/auth-guard.test.tsx
  - apps/web/src/test/venue-dislocation.test.tsx
  - apps/web/src/test/exposure-widget.test.tsx
  - apps/webhook_server/tests/unit/api/test_dislocation_endpoint.py
  - apps/webhook_server/tests/unit/api/test_exposure_endpoint.py
  - apps/webhook_server/tests/unit/api/test_pm_correlation_endpoint.py
  - packages/venue_adapters/tests/test_snapshot_store_supabase.py
  - apps/mobile/test/app_state_v1_test.dart
  - apps/mobile/test/offline_cache_test.dart
  - apps/bot/tests/test_fcm_ordering.py
  - packages/agent_pipeline/tests/test_copilot_tools_count.py
  - tests/integration/test_rls_endpoints.py
  - packages/venue_adapters/tests/test_settlement_ledger_supabase.py
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03
  - WIRE-04
  - WIRE-05
  - WIRE-06

must_haves:
  truths:
    - "Every WIRE requirement has a failing (RED) automated test before any implementation begins"
    - "All 14 test stub files exist with import stubs — no test passes yet, none are import errors"
    - "Running the full test suite reports N failures on identifiable test IDs, not import errors"
  artifacts:
    - path: "apps/web/src/test/auth.test.tsx"
      provides: "RED stubs for WIRE-01 login page behavior"
      min_lines: 25
    - path: "apps/web/src/test/auth-guard.test.tsx"
      provides: "RED stubs for WIRE-01 dashboard auth redirect"
      min_lines: 20
    - path: "apps/web/src/test/venue-dislocation.test.tsx"
      provides: "RED stubs for WIRE-02 dislocation widget"
      min_lines: 20
    - path: "apps/web/src/test/exposure-widget.test.tsx"
      provides: "RED stubs for WIRE-02 exposure widget"
      min_lines: 20
    - path: "apps/webhook_server/tests/unit/api/test_dislocation_endpoint.py"
      provides: "RED stubs for WIRE-02 GET /api/v1/markets/dislocation"
      min_lines: 30
    - path: "apps/webhook_server/tests/unit/api/test_exposure_endpoint.py"
      provides: "RED stubs for WIRE-02 GET /api/v1/bankroll/exposure"
      min_lines: 25
    - path: "apps/webhook_server/tests/unit/api/test_pm_correlation_endpoint.py"
      provides: "RED stubs for WIRE-04 GET /api/v1/prediction-markets/correlation"
      min_lines: 25
    - path: "packages/venue_adapters/tests/test_snapshot_store_supabase.py"
      provides: "RED stubs for WIRE-03 SnapshotStore real Supabase mode"
      min_lines: 35
    - path: "apps/mobile/test/app_state_v1_test.dart"
      provides: "RED stubs for WIRE-04 AppState v1 migration"
      min_lines: 30
    - path: "apps/mobile/test/offline_cache_test.dart"
      provides: "RED stubs for WIRE-04 offline cache behavior"
      min_lines: 25
    - path: "apps/bot/tests/test_fcm_ordering.py"
      provides: "RED stub for WIRE-05 FCM fires before Discord"
      min_lines: 25
    - path: "packages/agent_pipeline/tests/test_copilot_tools_count.py"
      provides: "RED stub for WIRE-06 12-tool assertion"
      min_lines: 20
    - path: "tests/integration/test_rls_endpoints.py"
      provides: "Wave 0 RED stub for WIRE-03 RLS JWT integration tests (6 endpoints)"
      min_lines: 30
    - path: "packages/venue_adapters/tests/test_settlement_ledger_supabase.py"
      provides: "Wave 0 RED stub for WIRE-03 LedgerStore Supabase mode"
      min_lines: 25
  key_links:
    - from: "apps/web/src/test/auth.test.tsx"
      to: "apps/web/src/app/auth/login/page.tsx"
      via: "import path in test"
      pattern: "from.*auth/login"
    - from: "apps/bot/tests/test_fcm_ordering.py"
      to: "apps/bot/src/sharpedge_bot/jobs/value_scanner_job.py"
      via: "import value_scanner_job"
      pattern: "import.*value_scanner_job"
---

<objective>
Create all RED TDD test stubs for Phase 8 before any implementation begins. Every WIRE requirement (WIRE-01 through WIRE-06) is locked by at least one failing test. Tests must fail due to missing implementation, not import errors.

Purpose: Lock interface contracts so Wave 1–4 implementations are verifiable the moment they land.
Output: 14 test stub files with clearly named test functions that fail with AssertionError or NotImplementedError — never ImportError.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-CONTEXT.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-RESEARCH.md
@.planning/phases/08-frontend-polish-and-full-backend-wiring/08-VALIDATION.md

<interfaces>
<!-- Key patterns from existing test files for consistency. Extracted from codebase. -->

From apps/web/src/test/dashboard.test.tsx (existing pattern):
```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
// vi.mock('@/lib/supabase', ...) pattern used for auth mocking
```

From apps/webhook_server/tests/unit/api/test_rls.py (existing pattern):
```python
import pytest
from fastapi.testclient import TestClient
from sharpedge_webhooks.main import app
client = TestClient(app)
```

From packages/venue_adapters/tests/test_snapshot_store.py (existing pattern):
```python
from sharpedge_venue_adapters.snapshot_store import SnapshotStore
# Tests use in-memory mode (no env vars). Supabase mode tests need SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY.
```

From apps/bot/src/sharpedge_bot/jobs/value_scanner_job.py:
```python
# send_fcm_notifications_for_play(play) called at line 336
# _pending_value_alerts.append(play) called at line 337
# FCM is already called BEFORE Discord append — WIRE-05 just needs a test asserting this ordering
```

From packages/agent_pipeline/src/sharpedge_agent_pipeline/copilot/tools.py:
```python
COPILOT_TOOLS = [
    get_active_bets,
    get_portfolio_stats,
    analyze_game,
    search_value_plays,
    check_line_movement,
    get_sharp_indicators,
    estimate_bankroll_risk,
    get_prediction_market_edge,
    compare_books,
    get_model_predictions,
] + VENUE_TOOLS  # adds get_venue_dislocation, get_exposure_status = 12 total
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Web + Python RED test stubs (WIRE-01, WIRE-02, WIRE-03, WIRE-05, WIRE-06)</name>
  <files>
    apps/web/src/test/auth.test.tsx,
    apps/web/src/test/auth-guard.test.tsx,
    apps/web/src/test/venue-dislocation.test.tsx,
    apps/web/src/test/exposure-widget.test.tsx,
    apps/webhook_server/tests/unit/api/test_dislocation_endpoint.py,
    apps/webhook_server/tests/unit/api/test_exposure_endpoint.py,
    apps/webhook_server/tests/unit/api/test_pm_correlation_endpoint.py,
    packages/venue_adapters/tests/test_snapshot_store_supabase.py,
    apps/bot/tests/test_fcm_ordering.py,
    packages/agent_pipeline/tests/test_copilot_tools_count.py,
    tests/integration/test_rls_endpoints.py,
    packages/venue_adapters/tests/test_settlement_ledger_supabase.py
  </files>
  <behavior>
    Web auth stubs (auth.test.tsx):
    - test_login_page_renders: renders email + password inputs — FAILS (LoginPage not yet created)
    - test_sign_in_with_password_called: submitting form calls supabase.auth.signInWithPassword — FAILS

    Web auth-guard stubs (auth-guard.test.tsx):
    - test_dashboard_redirects_when_no_session: unauthenticated user is redirected to /auth/login — FAILS

    Web venue-dislocation stubs (venue-dislocation.test.tsx):
    - test_venue_dislocation_widget_renders: VenueDislocWidget renders given dislocation data shape — FAILS (component not created)

    Web exposure-widget stubs (exposure-widget.test.tsx):
    - test_exposure_widget_renders_utilization_bars: ExposureWidget renders venue rows — FAILS

    Python dislocation endpoint (test_dislocation_endpoint.py):
    - test_get_dislocation_returns_200: GET /api/v1/markets/dislocation?market_id=X — expects 200 + {consensus_prob, scores} schema — FAILS (route not registered)
    - test_get_dislocation_requires_market_id: missing market_id returns 422 — FAILS

    Python exposure endpoint (test_exposure_endpoint.py):
    - test_get_exposure_returns_200: GET /api/v1/bankroll/exposure — expects 200 + {total_exposure, bankroll, venues} — FAILS

    Python PM correlation endpoint (test_pm_correlation_endpoint.py):
    - test_pm_correlation_returns_200: GET /api/v1/prediction-markets/correlation — expects 200 + list schema — FAILS

    SnapshotStore Supabase mode (test_snapshot_store_supabase.py):
    - test_supabase_mode_requires_env_vars: SnapshotStore with SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY — asserts _supabase is not None — FAILS (env vars absent in CI)
    - test_record_supabase_upsert_called: record() calls supabase.table().upsert() when in supabase mode — FAILS (no mock setup yet; skip marker with pytest.mark.supabase_integration)

    RLS endpoints integration (tests/integration/test_rls_endpoints.py) — Wave 0 RED stub:
    - Create file with skipif guard: pytest.mark.skipif(not os.getenv("SUPABASE_URL"), reason="integration test — requires SUPABASE_URL")
    - 6 stub test functions (raise NotImplementedError to ensure RED state): test_value_plays_requires_jwt, test_game_analysis_requires_jwt, test_copilot_requires_jwt, test_portfolio_requires_jwt, test_bankroll_simulate_requires_jwt, test_dislocation_requires_jwt
    - Each stub: `raise NotImplementedError("WIRE-03 RED stub — implement in Wave 1")`
    - Stubs skipped in CI (no SUPABASE_URL) — unskipped and implemented in 08-02

    LedgerStore Supabase mode (packages/venue_adapters/tests/test_settlement_ledger_supabase.py) — Wave 0 RED stub:
    - Create file with skipif guard: pytest.mark.skipif(not os.getenv("SUPABASE_URL"), reason="integration test — requires SUPABASE_URL")
    - 2 stub test functions (raise NotImplementedError): test_ledger_store_supabase_mode_not_none, test_ledger_store_record_calls_insert
    - Each stub: `raise NotImplementedError("WIRE-03 RED stub — implement in Wave 1")`
    - Stubs skipped in CI (no SUPABASE_URL)

    FCM ordering (test_fcm_ordering.py):
    - test_fcm_called_before_discord_append: mock send_fcm_notifications_for_play + _pending_value_alerts; assert FCM mock called before append — this test should PASS since FCM is already before append in value_scanner_job.py. Write it as a proper GREEN test. This is a verification test, not a RED stub.

    Copilot tools count (test_copilot_tools_count.py):
    - test_copilot_tools_has_12_entries: len(COPILOT_TOOLS) == 12 — this should also PASS since tools are already assembled. Write as a GREEN verification test.
  </behavior>
  <action>
    Create each test file. For RED stubs:
    - Web tests: use `vi.mock('@/lib/supabase', () => ({ supabase: { auth: { signInWithPassword: vi.fn(), getSession: vi.fn().mockResolvedValue({ data: { session: null } }) }}}))`. Import the not-yet-created component paths so tests fail on missing module or assertion.
    - Python tests: use `from fastapi.testclient import TestClient` from `sharpedge_webhooks.main import app`. Assert `response.status_code == 200` on routes that don't exist yet — the TestClient will return 404, making the assertion FAIL.
    - SnapshotStore Supabase test: mark with `@pytest.mark.skip(reason="WIRE-03 RED stub — requires real Supabase env vars")` so it can be unskipped in Wave 1 when integration env is wired.
    - RLS endpoints stub (tests/integration/test_rls_endpoints.py): create the file with `import os, pytest` at the top. Add `pytestmark = pytest.mark.skipif(not os.getenv("SUPABASE_URL"), reason="integration test — requires SUPABASE_URL")`. Define 6 test functions each containing `raise NotImplementedError("WIRE-03 RED stub — implement in Wave 1")`.
    - LedgerStore stub (packages/venue_adapters/tests/test_settlement_ledger_supabase.py): same pattern — `pytestmark = pytest.mark.skipif(...)`, 2 test functions each raising NotImplementedError.
    - FCM ordering test: write as a proper mock-based unit test using `unittest.mock.patch`. Patch `value_scanner_job.send_fcm_notifications_for_play` and `value_scanner_job._pending_value_alerts`. Call a helper that triggers the dispatch loop logic. Assert FCM mock called before append.
    - Copilot tools count: import COPILOT_TOOLS and assert `len(COPILOT_TOOLS) == 12`.

    Do NOT import non-existent modules at module level in Python stubs — use `from ... import ...` inside test functions to keep import errors isolated to individual test functions, not file-level.

    For web stubs, import the component inside the test body using dynamic import or wrap in a try/expect(component).toBeDefined() — this ensures vitest fails the specific test, not the file.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge/apps/web && npm test -- --run src/test/auth.test.tsx src/test/auth-guard.test.tsx src/test/venue-dislocation.test.tsx src/test/exposure-widget.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 web + Python stub files exist. Running `cd apps/web && npm test -- --run src/test/auth.test.tsx` reports failures on specific test names (not import errors). Running `cd apps/webhook_server && python -m pytest tests/unit/api/test_dislocation_endpoint.py tests/unit/api/test_exposure_endpoint.py tests/unit/api/test_pm_correlation_endpoint.py -v` reports FAILED on each test. FCM ordering test and copilot tools count test PASS. RLS endpoint stubs and LedgerStore stubs exist and are skipped in CI (no SUPABASE_URL).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flutter RED test stubs (WIRE-04)</name>
  <files>
    apps/mobile/test/app_state_v1_test.dart,
    apps/mobile/test/offline_cache_test.dart
  </files>
  <behavior>
    AppState v1 migration (app_state_v1_test.dart):
    - test_refresh_calls_get_value_plays_v1: after setAuthenticated(), refresh() calls getValuePlaysV1 not getValuePlays — FAILS (AppState.refresh() still calls old stub route)
    - test_refresh_forwards_auth_token: refresh() passes _authToken to API calls — FAILS

    Offline cache (offline_cache_test.dart):
    - test_offline_cache_returns_last_feed_on_network_failure: when API throws SocketException, AppState returns last cached feed — FAILS (no cache implemented)
    - test_cache_written_after_successful_refresh: after successful refresh(), SharedPreferences key 'cached_value_plays' is populated — FAILS
  </behavior>
  <action>
    Create both Dart test files using the `flutter_test` package. Use `MockApiService` via Mockito or manual stub class. Import from `package:sharpedge_mobile/providers/app_state.dart` (adjust if package name differs — check pubspec.yaml).

    In app_state_v1_test.dart:
    - Create a MockApiService that tracks which method was called
    - Instantiate AppState with the mock, call setAuthenticated then refresh()
    - Assert mockApiService.getValuePlaysV1WasCalled == true and getValuePlaysCalled == false

    In offline_cache_test.dart:
    - Inject MockApiService that throws SocketException
    - Pre-populate SharedPreferences with fake feed JSON
    - Call refresh() and assert valuePlays is non-empty (loaded from cache)

    These tests fail immediately since AppState.refresh() calls the old stub routes. That is the correct RED state.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge/apps/mobile && flutter test test/app_state_v1_test.dart test/offline_cache_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>Both Dart test files exist and compile. `flutter test test/app_state_v1_test.dart` reports test failures (not compilation errors) for the v1 migration assertions.</done>
</task>

</tasks>

<verification>
All 14 test stub files exist with identifiable failing test functions.

Python stubs: `cd /Users/revph/sharpedge/apps/webhook_server && python -m pytest tests/unit/api/test_dislocation_endpoint.py tests/unit/api/test_exposure_endpoint.py tests/unit/api/test_pm_correlation_endpoint.py -v 2>&1 | tail -20`

Bot FCM test (should PASS): `cd /Users/revph/sharpedge/apps/bot && python -m pytest tests/test_fcm_ordering.py -v`

Copilot count test (should PASS): `cd /Users/revph/sharpedge && uv run pytest packages/agent_pipeline/tests/test_copilot_tools_count.py -v`

RLS stubs (should be SKIPPED in CI): `cd /Users/revph/sharpedge && python -m pytest tests/integration/test_rls_endpoints.py -v`

LedgerStore stubs (should be SKIPPED in CI): `cd /Users/revph/sharpedge && python -m pytest packages/venue_adapters/tests/test_settlement_ledger_supabase.py -v`

Flutter stubs: `cd /Users/revph/sharpedge/apps/mobile && flutter test test/app_state_v1_test.dart test/offline_cache_test.dart`
</verification>

<success_criteria>
- 14 test stub files exist in their designated locations
- RED stubs fail on assertion (not ImportError or compile error)
- FCM ordering test and copilot tools count test are GREEN (pre-verified behaviors)
- RLS endpoint stubs and LedgerStore stubs are SKIPPED in CI (skipif guard on SUPABASE_URL)
- No production code modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-polish-and-full-backend-wiring/08-01-SUMMARY.md`
</output>
