---
phase: 09-prediction-market-resolution-models-and-expansion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/models/test_pm_feature_assembler.py
  - tests/unit/models/test_pm_resolution_predictor.py
  - tests/unit/scripts/test_download_pm_historical.py
  - tests/unit/scripts/test_process_pm_historical.py
  - tests/unit/scripts/test_train_pm_models.py
  - tests/unit/feeds/test_coingecko_client.py
  - tests/unit/feeds/test_fec_client.py
  - tests/unit/feeds/test_bls_client.py
autonomous: true
requirements:
  - PM-DATA-01
  - PM-DATA-02
  - PM-RES-01
  - PM-RES-02
  - PM-INT-01

must_haves:
  truths:
    - "All 8 test files import without error (stubs define the interface contract)"
    - "PMFeatureAssembler stub raises NotImplementedError for assemble()"
    - "PMResolutionPredictor stub returns empty dict from build_model_probs() (correct fail-safe default)"
    - "CoinGeckoClient, FECClient, BLSClient stubs raise NotImplementedError for their fetch methods"
    - "All 8 test files fail with assertion errors (RED state), not import errors"
  artifacts:
    - path: "tests/unit/models/test_pm_feature_assembler.py"
      provides: "PMFeatureAssembler interface contract"
    - path: "tests/unit/models/test_pm_resolution_predictor.py"
      provides: "PMResolutionPredictor interface contract"
    - path: "tests/unit/feeds/test_coingecko_client.py"
      provides: "CoinGeckoClient interface contract"
    - path: "tests/unit/feeds/test_fec_client.py"
      provides: "FECClient interface contract"
    - path: "tests/unit/feeds/test_bls_client.py"
      provides: "BLSClient interface contract"
  key_links:
    - from: "PMResolutionPredictor.build_model_probs()"
      to: "pm_edge_scanner.scan_pm_edges(model_probs=...)"
      via: "dict[str, float] return value"
      pattern: "build_model_probs.*dict\\[str.*float\\]"
    - from: "PMFeatureAssembler.assemble()"
      to: "PMResolutionPredictor.predict()"
      via: "feature vector np.ndarray"
      pattern: "assemble.*ndarray"
---

<objective>
Write failing RED test stubs that lock the interface contracts for all Phase 9 components before any implementation begins.

Purpose: TDD contract lock — prevents interface drift as implementation proceeds across plans 02–05.
Output: 8 test files covering PMFeatureAssembler, PMResolutionPredictor, download/process/train scripts, and 3 new API clients.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-prediction-market-resolution-models-and-expansion/09-CONTEXT.md
@.planning/phases/09-prediction-market-resolution-models-and-expansion/09-RESEARCH.md

<interfaces>
<!-- Existing contracts that Phase 9 must integrate with. -->

From packages/analytics/src/sharpedge_analytics/pm_edge_scanner.py:
```python
def scan_pm_edges(
    kalshi_markets: list,
    polymarket_markets: list,
    model_probs: dict[str, float],   # market_id → ML-predicted probability; missing key → fee-adjusted fallback
    volume_floor: float = 1000.0,
    active_bets: list | None = None,
    market_titles: dict | None = None,
) -> list:  # list[PMEdge | CorrelationWarning]
```

From packages/models/src/sharpedge_models/calibration_store.py:
```python
class CalibrationStore:
    def update(self, sport: str, predictions: list[float], actuals: list[int]) -> None: ...
    def confidence_mult(self, sport: str) -> float: ...
```

From packages/models/src/sharpedge_models/walk_forward.py:
```python
def quality_badge_from_windows(windows: list) -> str:  # "low"|"medium"|"high"|"excellent"
```

From packages/data_feeds/src/sharpedge_feeds/kalshi_client.py:
```python
@dataclass
class KalshiMarket:
    ticker: str
    event_ticker: str
    title: str
    yes_bid: float
    yes_ask: float
    volume: float
    open_interest: float
    last_price: float
    close_time: datetime
    result: str | None  # "yes" | "no" | None
```

From packages/data_feeds/src/sharpedge_feeds/polymarket_client.py:
```python
@dataclass
class PolymarketMarket:
    condition_id: str
    question: str
    outcomes: list  # list[PolymarketOutcome]
    volume: float
    liquidity: float
    end_date: datetime
    category: str
    closed: bool

@dataclass
class PolymarketOutcome:
    token_id: str
    outcome: str   # "Yes" | "No"
    price: float   # 0–1
    winner: bool | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs — PMFeatureAssembler + PMResolutionPredictor</name>
  <files>
    tests/unit/models/test_pm_feature_assembler.py
    tests/unit/models/test_pm_resolution_predictor.py
    packages/models/src/sharpedge_models/pm_feature_assembler.py
    packages/models/src/sharpedge_models/pm_resolution_predictor.py
  </files>
  <behavior>
    PMFeatureAssembler tests:
    - test_assemble_universal_features: given a market dict with market_prob=0.6, bid_ask_spread=0.04, last_price=0.58, volume=5000, open_interest=200, days_to_close=7 → assemble() returns np.ndarray of length >= 6
    - test_assemble_political_features: political category market → array length >= 8 (6 universal + 2 add-ons: polling_average, election_proximity_days)
    - test_assemble_economic_features: economic category → array length >= 8 (6 universal + days_since_last_release + is_release_imminent)
    - test_assemble_crypto_features: crypto category → array length >= 8 (6 universal + underlying_asset_price + price_change_7d)
    - test_assemble_entertainment_features: entertainment category → array length == 6 (universal only)
    - test_assemble_weather_features: weather category → array length == 6 (universal only)
    - test_detect_category_political: market with event_ticker starting "KXPOL" or question containing "election" → category == "political"
    - test_detect_category_crypto: market with event_ticker "KXBTC" or question containing "bitcoin"/"ETH" → category == "crypto"
    - test_detect_category_economic: market with event_ticker "KXFED" or question containing "CPI"/"inflation"/"rate" → category == "economic"
    - test_detect_category_entertainment: market with question containing "oscar"/"grammy"/"award" → category == "entertainment"
    - test_detect_category_weather: market with question containing "hurricane"/"temperature"/"rainfall" → category == "weather"
    - test_unknown_category_defaults_to_entertainment: unrecognized question/ticker → category == "entertainment" (market-only fallback)

    PMResolutionPredictor tests:
    - test_flag_off_returns_empty: ENABLE_PM_RESOLUTION_MODEL not set → build_model_probs([]) == {}
    - test_flag_on_missing_model_file_returns_empty_for_category: flag on, no joblib file for "crypto" → "crypto" category markets not in returned dict (falls back to fee-adjusted)
    - test_flag_on_with_model_returns_probabilities: flag on, mock joblib load returns fitted model → dict[market_id, float] with probabilities in (0, 1)
    - test_skipped_category_not_in_output: category with no model artifact → its market_ids absent from returned dict
    - test_no_exceptions_on_missing_models: build_model_probs() with all models missing → returns {} without raising
  </behavior>
  <action>
    Create stub implementations FIRST (so tests can import without ImportError), then write failing tests.

    STUB: packages/models/src/sharpedge_models/pm_feature_assembler.py
    - `CATEGORY_KEYWORDS: dict[str, list[str]]` mapping category → keyword list (for detect_category)
    - `PM_UNIVERSAL_FEATURES = ["market_prob", "bid_ask_spread", "last_price", "volume", "open_interest", "days_to_close"]` — module-level constant
    - `PMFeatureAssembler` class with:
      - `detect_category(market: dict) -> str` — checks event_ticker prefix and question keywords; returns one of "political"|"economic"|"crypto"|"entertainment"|"weather"; defaults to "entertainment"
      - `assemble(market: dict) -> np.ndarray` — raise NotImplementedError("implement in plan 03")
    - Keep file under 100 lines (stub only)

    STUB: packages/models/src/sharpedge_models/pm_resolution_predictor.py
    - `ENABLE_FLAG = "ENABLE_PM_RESOLUTION_MODEL"` — module-level constant
    - `PM_MODEL_DIR = Path("data/models/pm")` — module-level constant
    - `PMResolutionPredictor` class with:
      - `build_model_probs(markets: list[dict]) -> dict[str, float]` — return {} (correct safe default)
    - Keep file under 80 lines (stub only)

    TEST FILES: write tests that call the stub and assert the behaviors listed in `<behavior>` above. Tests MUST fail (RED) because assemble() raises NotImplementedError and build_model_probs() returns {} when tests expect non-empty output.

    Import path: `from sharpedge_models.pm_feature_assembler import PMFeatureAssembler`
    Import path: `from sharpedge_models.pm_resolution_predictor import PMResolutionPredictor`
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && python -m pytest tests/unit/models/test_pm_feature_assembler.py tests/unit/models/test_pm_resolution_predictor.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files collected, imports succeed, tests fail with NotImplementedError or AssertionError (RED state, not ImportError)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED stubs — 3 API clients + download/process/train scripts</name>
  <files>
    tests/unit/feeds/test_coingecko_client.py
    tests/unit/feeds/test_fec_client.py
    tests/unit/feeds/test_bls_client.py
    tests/unit/scripts/test_download_pm_historical.py
    tests/unit/scripts/test_process_pm_historical.py
    tests/unit/scripts/test_train_pm_models.py
    packages/data_feeds/src/sharpedge_feeds/coingecko_client.py
    packages/data_feeds/src/sharpedge_feeds/fec_client.py
    packages/data_feeds/src/sharpedge_feeds/bls_client.py
  </files>
  <behavior>
    CoinGeckoClient tests:
    - test_get_price_returns_float: get_price("bitcoin") → float in (0, ∞)
    - test_get_price_change_7d_returns_float: get_price_change_7d("bitcoin") → float
    - test_offline_mode_returns_defaults: COINGECKO_OFFLINE=true → returns (0.0, 0.0) without network call

    FECClient tests:
    - test_get_polling_average_returns_float: get_polling_average("presidential-2024") → float in [0, 1]
    - test_get_election_proximity_days_returns_int: get_election_proximity_days("2024-11-05") → int >= 0
    - test_offline_mode_returns_defaults: FEC_OFFLINE=true → returns (0.0, 365) without network call

    BLSClient tests:
    - test_get_days_since_last_release_returns_int: get_days_since_last_release("CPI") → int >= 0
    - test_get_is_release_imminent_returns_bool: get_is_release_imminent("CPI", threshold_days=3) → bool
    - test_offline_mode_returns_defaults: BLS_OFFLINE=true → returns (30, False) without network call

    download_pm_historical.py tests (import + entrypoint):
    - test_backfill_kalshi_resolved_returns_dataframe: mock KalshiClient.get_markets → returns non-empty DataFrame with columns [ticker, result, volume, open_interest, last_price, yes_bid, yes_ask, close_time, event_ticker]
    - test_backfill_polymarket_resolved_returns_dataframe: mock PolymarketClient.get_markets → DataFrame with columns [condition_id, question, volume, liquidity, end_date, category, resolved_yes]
    - test_offline_mode_returns_fixture_data: KALSHI_API_KEY absent → returns fixture DataFrame (50 rows)

    process_pm_historical.py tests:
    - test_process_kalshi_adds_universal_features: given raw parquet → output DataFrame has all 6 universal feature columns
    - test_process_polymarket_adds_universal_features: given raw parquet → output DataFrame has all 6 universal feature columns
    - test_low_data_category_filtered_count: category with < 200 markets in processed data → logged to JSON report with "skipped": true

    train_pm_models.py tests:
    - test_train_skips_category_below_200: category with 150 rows → no model artifact written, JSON report entry has "skipped": true
    - test_train_produces_joblib_artifact: category with 300 rows (mocked) → data/models/pm/{category}.joblib written
    - test_walk_forward_requires_3_windows: category with < 150 markets (3 × 50) → quality_badge returns "low", category skipped
  </behavior>
  <action>
    Create 3 API client STUBS first, then write failing tests. Script tests mock at subprocess or function level.

    STUB: packages/data_feeds/src/sharpedge_feeds/coingecko_client.py
    - `COINGECKO_BASE = "https://api.coingecko.com/api/v3"` — module-level
    - `CoinGeckoClient` class:
      - `get_price(coin_id: str) -> float` — raise NotImplementedError
      - `get_price_change_7d(coin_id: str) -> float` — raise NotImplementedError
    - Keep under 80 lines

    STUB: packages/data_feeds/src/sharpedge_feeds/fec_client.py
    - `FECClient` class:
      - `get_polling_average(race_id: str) -> float` — raise NotImplementedError
      - `get_election_proximity_days(election_date_str: str) -> int` — raise NotImplementedError
    - Keep under 80 lines

    STUB: packages/data_feeds/src/sharpedge_feeds/bls_client.py
    - `BLS_RELEASE_CALENDAR_URL = "https://www.bls.gov/schedule/news_release/"` — module-level
    - `BLSClient` class:
      - `get_days_since_last_release(series: str) -> int` — raise NotImplementedError
      - `get_is_release_imminent(series: str, threshold_days: int = 3) -> bool` — raise NotImplementedError
    - Keep under 80 lines

    NOTE: scripts/download_pm_historical.py, scripts/process_pm_historical.py, and scripts/train_pm_models.py do NOT exist yet. Test files for them import from sharpedge_models or mock at the subprocess level. Use `pytest.importorskip` pattern or structure tests as importable functions (not __main__ only) so they can be mocked.

    For script tests, the test files import the script as a module function (not subprocess). Script stubs are not created in this plan — the test files use `pytest.mark.xfail(reason="plan 02/04 implementation pending")` on all assertions so they are RED stubs that document the contract without requiring the scripts to exist yet.

    All 3 client stubs: update packages/data_feeds/src/sharpedge_feeds/__init__.py to export the 3 new clients (append, do not replace).
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && python -m pytest tests/unit/feeds/test_coingecko_client.py tests/unit/feeds/test_fec_client.py tests/unit/feeds/test_bls_client.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 test files collected without ImportError. 3 client test files fail RED (NotImplementedError). Script test files marked xfail (documented contracts). 3 client stubs exist and are importable.</done>
</task>

</tasks>

<verification>
Run full RED stub check:
```
cd /Users/revph/sharpedge && python -m pytest tests/unit/models/test_pm_feature_assembler.py tests/unit/models/test_pm_resolution_predictor.py tests/unit/feeds/ -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR|xfail" | head -40
```

All tests must be collectable (no ImportError). Failures must be NotImplementedError or AssertionError, never ImportError.
</verification>

<success_criteria>
- 8 test files exist and are collected by pytest
- 5 stub files exist and are importable (pm_feature_assembler, pm_resolution_predictor, coingecko_client, fec_client, bls_client)
- PMFeatureAssembler.detect_category() is implemented (GREEN) — only assemble() raises NotImplementedError
- PMResolutionPredictor.build_model_probs() returns {} (correct safe default for RED state)
- All tests are RED (fail) or xfail — no GREEN tests (no premature implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/09-prediction-market-resolution-models-and-expansion/09-01-SUMMARY.md`
</output>
