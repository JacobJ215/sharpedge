---
phase: 04-api-layer-front-ends
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/webhook_server/tests/unit/api/__init__.py
  - apps/webhook_server/tests/unit/api/test_rls.py
  - apps/webhook_server/tests/unit/api/test_value_plays_v1.py
  - apps/webhook_server/tests/unit/api/test_game_analysis.py
  - apps/webhook_server/tests/unit/api/test_copilot_sse.py
  - apps/webhook_server/tests/unit/api/test_portfolio.py
  - apps/webhook_server/tests/unit/api/test_bankroll_simulate.py
  - scripts/schema_rls.sql
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06

must_haves:
  truths:
    - "All 6 API test files exist and fail with ImportError or NotImplementedError (RED state)"
    - "Next.js 14 project exists at apps/web/ with App Router, TypeScript, Tailwind, shadcn/ui, Recharts, and vitest"
    - "RLS migration SQL script exists with ENABLE ROW LEVEL SECURITY for all user-scoped tables"
    - "user_device_tokens table DDL exists for FCM token storage"
  artifacts:
    - path: "apps/webhook_server/tests/unit/api/__init__.py"
      provides: "API test package"
    - path: "apps/webhook_server/tests/unit/api/test_rls.py"
      provides: "RED stubs for API-06"
    - path: "apps/webhook_server/tests/unit/api/test_value_plays_v1.py"
      provides: "RED stubs for API-01"
    - path: "apps/webhook_server/tests/unit/api/test_copilot_sse.py"
      provides: "RED stubs for API-03"
    - path: "apps/web/package.json"
      provides: "Next.js project"
    - path: "apps/web/vitest.config.ts"
      provides: "vitest config for React testing"
    - path: "scripts/schema_rls.sql"
      provides: "RLS enable + policy SQL for all tables"
  key_links:
    - from: "apps/webhook_server/tests/unit/api/"
      to: "apps/webhook_server/src/sharpedge_webhooks/routes/v1/"
      via: "import path that must fail RED until Wave 1 implements"
      pattern: "from sharpedge_webhooks\\.routes\\.v1"
---

<objective>
Wave 0: Create all prerequisite infrastructure that downstream plans depend on. Produces RED test stubs (ImportError = correct RED state) for all 6 FastAPI v1 endpoints, scaffolds the Next.js 14 web app with vitest, and writes the Supabase RLS migration SQL.

Purpose: Lock API contracts before implementation. Nyquist compliance requires test files exist before Wave 1 runs. Next.js scaffold ensures Wave 2 web plans have a real project to extend.
Output: 7 Python test files (all RED), Next.js 14 project at apps/web/, RLS SQL migration script.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-layer-front-ends/04-CONTEXT.md
@.planning/phases/04-api-layer-front-ends/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test stubs for all 6 FastAPI v1 endpoints</name>
  <files>
    apps/webhook_server/tests/unit/api/__init__.py,
    apps/webhook_server/tests/unit/api/test_rls.py,
    apps/webhook_server/tests/unit/api/test_value_plays_v1.py,
    apps/webhook_server/tests/unit/api/test_game_analysis.py,
    apps/webhook_server/tests/unit/api/test_copilot_sse.py,
    apps/webhook_server/tests/unit/api/test_portfolio.py,
    apps/webhook_server/tests/unit/api/test_bankroll_simulate.py
  </files>
  <action>
    Create the API test package and 6 RED stub files. The import path `from sharpedge_webhooks.routes.v1 import ...` does not exist yet — that is intentional; tests must fail with ImportError.

    `apps/webhook_server/tests/unit/api/__init__.py` — empty file.

    `test_rls.py` — stubs for API-06. Import `router` from `sharpedge_webhooks.routes.v1.value_plays` (will ImportError). Two tests:
    - `test_rls_blocks_cross_user_portfolio` — calls GET /api/v1/users/{other_user_id}/portfolio with a different user's JWT, asserts response.status_code == 403.
    - `test_rls_allows_own_portfolio` — calls GET /api/v1/users/{own_id}/portfolio with correct JWT, asserts 200.
    Use `pytest.raises(ImportError)` wrapper pattern OR mark with `pytest.mark.skip(reason="RED — routes/v1 not yet implemented")` so collection succeeds but tests fail meaningfully. Prefer the skip pattern for cleaner RED state.

    `test_value_plays_v1.py` — stubs for API-01. Import from `sharpedge_webhooks.routes.v1.value_plays`. Three tests:
    - `test_value_plays_returns_alpha_fields` — asserts response JSON contains `alpha_score`, `alpha_badge`, `regime_state`.
    - `test_value_plays_min_alpha_filter` — calls with `?min_alpha=0.7`, asserts all returned items have `alpha_score >= 0.7`.
    - `test_value_plays_badge_values` — asserts `alpha_badge` is one of PREMIUM/HIGH/MEDIUM/SPECULATIVE.
    Skip all with `reason="RED — routes/v1/value_plays not yet implemented"`.

    `test_game_analysis.py` — stubs for API-02. Two tests:
    - `test_game_analysis_returns_full_state` — GET /api/v1/games/{game_id}/analysis, asserts response contains `model_prediction`, `ev_breakdown`, `regime_state`, `key_number_proximity`.
    - `test_game_analysis_404_unknown_game` — GET /api/v1/games/nonexistent/analysis, asserts 404.

    `test_copilot_sse.py` — stubs for API-03. Two tests:
    - `test_copilot_sse_content_type` — POST /api/v1/copilot/chat with `{"message": "hello"}`, asserts response Content-Type is `text/event-stream`.
    - `test_copilot_sse_streams_data` — asserts response body starts with `data:` (SSE format).

    `test_portfolio.py` — stubs for API-04. Two tests:
    - `test_portfolio_shape` — GET /api/v1/users/{id}/portfolio, asserts response has `roi`, `win_rate`, `clv_average`, `drawdown`, `active_bets`.
    - `test_portfolio_requires_auth` — GET without Authorization header, asserts 401.

    `test_bankroll_simulate.py` — stubs for API-05. Two tests:
    - `test_bankroll_simulate_returns_monte_carlo` — POST /api/v1/bankroll/simulate with `{"bankroll": 1000, "bet_size": 50, "num_bets": 100, "win_rate": 0.55}`, asserts response has `ruin_probability`, `p5_outcome`, `p50_outcome`, `p95_outcome`, `max_drawdown`.
    - `test_bankroll_simulate_no_auth_required` — same POST without auth header, asserts 200 (this endpoint is public).

    NOTE: All test functions use `pytest.mark.skip(reason="RED — ...")` so they collect but do not fail on import.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge/apps/webhook_server && uv run pytest tests/unit/api/ -v --collect-only 2>&1 | tail -20</automated>
  </verify>
  <done>7 test files collected, 0 errors during collection, all tests in SKIPPED state (RED stubs confirmed)</done>
</task>

<task type="auto">
  <name>Task 2: Next.js 14 scaffold at apps/web/ with vitest + RLS SQL script</name>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/tailwind.config.ts,
    apps/web/next.config.ts,
    apps/web/vitest.config.ts,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    scripts/schema_rls.sql
  </files>
  <action>
    PART A — Next.js 14 scaffold.

    Create `apps/web/` as a Next.js 14 project with TypeScript, Tailwind CSS, and App Router. Do NOT run `create-next-app` interactively; create the files directly.

    `apps/web/package.json`:
    ```json
    {
      "name": "sharpedge-web",
      "version": "0.1.0",
      "private": true,
      "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "test": "vitest run",
        "test:watch": "vitest"
      },
      "dependencies": {
        "next": "14.2.5",
        "react": "^18.3.1",
        "react-dom": "^18.3.1",
        "@supabase/supabase-js": "^2.45.0",
        "recharts": "^2.12.7",
        "swr": "^2.2.5",
        "clsx": "^2.1.1",
        "tailwind-merge": "^2.5.2"
      },
      "devDependencies": {
        "typescript": "^5",
        "@types/node": "^20",
        "@types/react": "^18",
        "@types/react-dom": "^18",
        "tailwindcss": "^3.4.1",
        "postcss": "^8",
        "autoprefixer": "^10.0.1",
        "vitest": "^2.1.0",
        "@vitejs/plugin-react": "^4.3.1",
        "@testing-library/react": "^16.0.0",
        "@testing-library/jest-dom": "^6.5.0",
        "jsdom": "^25.0.0"
      }
    }
    ```

    `apps/web/tsconfig.json` — standard Next.js 14 tsconfig with `"paths": { "@/*": ["./src/*"] }`.

    `apps/web/next.config.ts`:
    ```typescript
    import type { NextConfig } from 'next'
    const nextConfig: NextConfig = {}
    export default nextConfig
    ```

    `apps/web/tailwind.config.ts` — standard Tailwind config with content paths: `./src/**/*.{ts,tsx}`.

    `apps/web/postcss.config.mjs` — standard postcss with tailwindcss + autoprefixer.

    `apps/web/src/app/layout.tsx` — root layout with `<html lang="en"><body>{children}</body></html>`, imports globals.css with Tailwind directives.

    `apps/web/src/app/globals.css` — `@tailwind base; @tailwind components; @tailwind utilities;`

    `apps/web/src/app/page.tsx` — placeholder: `export default function Home() { return <main><h1>SharpEdge</h1></main> }`

    `apps/web/vitest.config.ts`:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import { resolve } from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: ['./src/test-setup.ts'],
      },
      resolve: {
        alias: { '@': resolve(__dirname, './src') },
      },
    })
    ```

    `apps/web/src/test-setup.ts`:
    ```typescript
    import '@testing-library/jest-dom'
    ```

    PART B — Supabase RLS migration SQL.

    Create `scripts/schema_rls.sql` with:
    1. `ALTER TABLE bets ENABLE ROW LEVEL SECURITY;`
    2. `ALTER TABLE user_bankroll ENABLE ROW LEVEL SECURITY;` (or whatever the bankroll table is named)
    3. `ALTER TABLE value_plays ENABLE ROW LEVEL SECURITY;` — if value_plays has user_id column, add user policy; if it's shared game data, use `service_role` only
    4. RLS policies for user-scoped tables: `CREATE POLICY "user_own_data" ON bets FOR ALL USING (auth.uid() = user_id);`
    5. Service role bypass: `CREATE POLICY "service_role_all" ON bets FOR ALL TO service_role USING (true);`
    6. `CREATE TABLE IF NOT EXISTS user_device_tokens (id UUID DEFAULT gen_random_uuid() PRIMARY KEY, user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, fcm_token TEXT NOT NULL, platform TEXT NOT NULL CHECK (platform IN ('ios', 'android')), created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now(), UNIQUE (user_id, fcm_token));`
    7. `ALTER TABLE user_device_tokens ENABLE ROW LEVEL SECURITY;`
    8. Policy: user can only read/write their own device tokens.

    Include tables: bets, user_bankroll, user_device_tokens. Add comment header noting this runs against Supabase SQL editor with service_role privileges.

    PART C — shadcn/ui setup stub.
    Create `apps/web/components.json` for shadcn/ui with style: "default", baseColor: "slate", cssVariables: true. This allows shadcn CLI to add components in Wave 2 plans without reconfiguring.

    DO NOT run `npm install` — executor will install during `npm run build` verification or explicitly if needed.
  </action>
  <verify>
    <automated>ls /Users/revph/sharpedge/apps/web/src/app/layout.tsx && ls /Users/revph/sharpedge/apps/web/vitest.config.ts && ls /Users/revph/sharpedge/scripts/schema_rls.sql && echo "scaffold OK"</automated>
  </verify>
  <done>apps/web/ directory exists with package.json, vitest.config.ts, src/app/layout.tsx; scripts/schema_rls.sql exists with RLS DDL for bets, user_bankroll, and user_device_tokens</done>
</task>

</tasks>

<verification>
After both tasks:
- `cd /Users/revph/sharpedge/apps/webhook_server && uv run pytest tests/unit/api/ --collect-only` — 6 test files, no collection errors
- `ls /Users/revph/sharpedge/apps/web/package.json` — exists
- `ls /Users/revph/sharpedge/scripts/schema_rls.sql` — exists
- `grep "ENABLE ROW LEVEL SECURITY" /Users/revph/sharpedge/scripts/schema_rls.sql | wc -l` — at least 3 tables
</verification>

<success_criteria>
- 7 Python test files collected (0 errors) in apps/webhook_server/tests/unit/api/
- All test functions skip with RED reason string (no import failures during collection)
- apps/web/ has valid Next.js 14 structure: package.json, tsconfig.json, vitest.config.ts, src/app/layout.tsx
- scripts/schema_rls.sql has ENABLE ROW LEVEL SECURITY for bets, user_bankroll, user_device_tokens
- user_device_tokens table DDL present with user_id FK, fcm_token TEXT, platform CHECK constraint
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-layer-front-ends/04-00-SUMMARY.md`
</output>
