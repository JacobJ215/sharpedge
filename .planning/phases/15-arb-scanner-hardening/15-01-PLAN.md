---
phase: 15-arb-scanner-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/analytics/tests/__init__.py
  - packages/analytics/tests/test_realtime_scanner.py
autonomous: true
requirements: [ARB-01, ARB-02, ARB-03, ARB-04]

must_haves:
  truths:
    - "All 8 RED test cases exist and fail with ImportError or NotImplementedError — not pass"
    - "Test file covers staleness guard (ARB-03), NO token real ask (ARB-04), auto-discovery (ARB-01), and dual order placement (ARB-02)"
    - "No production code is modified in this plan — tests only"
  artifacts:
    - path: "packages/analytics/tests/__init__.py"
      provides: "Makes tests/ a Python package so pytest discovers it"
      min_lines: 0
    - path: "packages/analytics/tests/test_realtime_scanner.py"
      provides: "8 failing test stubs — one per behavior from research validation table"
      min_lines: 80
  key_links:
    - from: "packages/analytics/tests/test_realtime_scanner.py"
      to: "packages/analytics/src/sharpedge_analytics/prediction_markets/realtime_scanner.py"
      via: "import RealtimeArbScanner, MarketPair"
      pattern: "from sharpedge_analytics.prediction_markets.realtime_scanner import"
---

<objective>
Create the full RED test suite for Phase 15 before any production code changes.

Purpose: TDD wave 0 — writing all 8 failing tests first locks the behavioral contracts and prevents scope creep during implementation.
Output: `packages/analytics/tests/test_realtime_scanner.py` with 8 failing tests covering all four ARB requirements.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-arb-scanner-hardening/15-RESEARCH.md
</context>

<feature>
  <name>Phase 15 RED test suite</name>
  <files>packages/analytics/tests/__init__.py, packages/analytics/tests/test_realtime_scanner.py</files>
  <behavior>
    TDD RED phase — all tests must fail (ImportError or AttributeError or assertion failure), not pass.

    Test functions to create (one per row in research validation table):

    ARB-03 staleness guard:
    - test_staleness_guard_kalshi: set pair.last_kalshi_ts = time.time() - 10, pair.last_poly_ts = time.time(), kalshi_yes_ask=0.45, poly_yes_ask=0.50; call _check_pair(pair); assert arb callback was NOT fired (stale Kalshi side skipped)
    - test_staleness_guard_poly: same but stale poly side (last_poly_ts = time.time() - 10)
    - test_staleness_guard_uninit: both timestamps = 0.0 (fresh pair, never ticked); call _check_pair with a profitable gap; assert staleness guard does NOT trigger (0.0 means "no data yet", not "infinitely stale")

    ARB-04 NO token real orderbook:
    - test_no_token_real_ask: MarketPair with polymarket_no_token=None and poly_no_ask=0; inject mock poly_client returning asks=[{"price":"0.47","size":"200"}]; call _check_pair; assert pair.poly_no_ask == 0.47 (real ask used, not 1 - yes_ask)
    - test_no_token_fallback: same setup but get_orderbook returns asks=[]; assert pair.poly_no_ask remains derived from 1 - poly_yes_ask (fallback preserved when no liquidity)

    ARB-01 auto-discovery:
    - test_discover_and_wire: mock kalshi_client.get_markets() returning one KalshiMarket; mock poly_client.get_markets() returning one PolymarketMarket with matching question text and both YES/NO tokens in outcomes; call await scanner.discover_and_wire(...); assert register_pair was called once and wire was called once
    - test_no_token_extraction: same but assert the registered MarketPair.polymarket_no_token matches the NO token_id from the poly market's outcomes array (not the YES token_id)

    ARB-02 dual order placement:
    - test_dual_order_placement: call shadow_execute_arb(opp, kalshi_client, poly_clob_client); assert both kalshi_client.create_order and poly_clob_client.place_order were called concurrently (both called, both order IDs recorded in return dict)

    All tests use pytest-asyncio for async tests. Use unittest.mock.AsyncMock for async client methods.
  </behavior>
  <implementation>
    RED phase only. Do not implement production code.

    File layout:
    1. `packages/analytics/tests/__init__.py` — empty file (touch).
    2. `packages/analytics/tests/test_realtime_scanner.py`:
       - imports: pytest, asyncio, time, unittest.mock (AsyncMock, MagicMock, patch), pytest_asyncio
       - import RealtimeArbScanner, MarketPair, LiveArbOpportunity from realtime_scanner
       - import PolymarketClient, PolymarketMarket, PolymarketOutcome from sharpedge_feeds.polymarket_client
       - import KalshiClient, KalshiMarket from sharpedge_feeds.kalshi_client
       - For ARB-02: try to import shadow_execute_arb from realtime_scanner (will fail — that's the RED state)
       - For ARB-04: try to import PolymarketCLOBOrderClient from sharpedge_feeds.polymarket_clob_orders (will fail — RED state)
       - Each test function is a standalone async def decorated with @pytest.mark.asyncio
       - Use `caplog` fixture for staleness warning assertions
       - Helper `_make_scanner()` creates RealtimeArbScanner(staleness_threshold_s=5.0, min_gap_pct=2.0, bankroll=10_000.0)
       - Helper `_make_pair(kalshi_ts=0.0, poly_ts=0.0, ...)` creates MarketPair with given timestamps and prices

    Run after writing:
    `uv run pytest packages/analytics/tests/test_realtime_scanner.py -x -q 2>&1 | head -40`
    All tests must show FAILED or ERROR — no PASSED tests are acceptable in RED phase.
  </implementation>
</feature>

<verification>
RED phase complete when:
- `uv run pytest packages/analytics/tests/test_realtime_scanner.py -q 2>&1 | tail -5` shows 8 failed/error, 0 passed
- No production file (realtime_scanner.py, polymarket_client.py) is modified
</verification>

<success_criteria>
8 test stubs exist and fail. Zero pass. Zero production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/15-arb-scanner-hardening/15-01-SUMMARY.md`
</output>
