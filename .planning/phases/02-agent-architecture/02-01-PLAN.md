---
phase: 02-agent-architecture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/agent_pipeline/pyproject.toml
  - packages/agent_pipeline/src/sharpedge_agent_pipeline/__init__.py
  - tests/unit/agent_pipeline/__init__.py
  - tests/unit/agent_pipeline/test_graph.py
  - tests/unit/agent_pipeline/test_state.py
  - tests/unit/agent_pipeline/test_validate_setup.py
  - tests/unit/agent_pipeline/test_copilot.py
  - tests/unit/agent_pipeline/test_copilot_tools.py
  - tests/unit/agent_pipeline/test_session.py
  - tests/unit/agent_pipeline/test_alpha_ranker.py
  - apps/bot/pyproject.toml
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "Package sharpedge-agent-pipeline directory exists with valid pyproject.toml listing langgraph>=1.1.0, langchain-openai, tiktoken, langgraph-checkpoint-postgres"
    - "All 7 test stub files exist under tests/unit/agent_pipeline/ and each contains at least one xfail or failing assertion"
    - "uv run pytest tests/unit/agent_pipeline/ -x -q exits non-zero (RED state — stubs fail by design)"
    - "apps/bot/pyproject.toml lists sharpedge-agent-pipeline as a dependency"
    - "Root workspace members = packages/* already covers agent_pipeline — no root pyproject.toml change needed"
  artifacts:
    - path: "packages/agent_pipeline/pyproject.toml"
      provides: "sharpedge-agent-pipeline package declaration"
      contains: "langgraph>=1.1.0"
    - path: "packages/agent_pipeline/src/sharpedge_agent_pipeline/__init__.py"
      provides: "package entry point (empty or stub)"
    - path: "tests/unit/agent_pipeline/test_graph.py"
      provides: "RED stubs for AGENT-01 graph routing + AGENT-02 retry-cap"
    - path: "tests/unit/agent_pipeline/test_state.py"
      provides: "RED stubs for AGENT-01 parallel state safety + quality_warnings reducer"
    - path: "tests/unit/agent_pipeline/test_validate_setup.py"
      provides: "RED stubs for AGENT-02 (mocked LLM)"
    - path: "tests/unit/agent_pipeline/test_copilot.py"
      provides: "RED stubs for AGENT-03 (mocked tools)"
    - path: "tests/unit/agent_pipeline/test_copilot_tools.py"
      provides: "RED stubs for AGENT-04 (mocked DB)"
    - path: "tests/unit/agent_pipeline/test_session.py"
      provides: "RED stubs for AGENT-04 trim_messages"
    - path: "tests/unit/agent_pipeline/test_alpha_ranker.py"
      provides: "RED stubs for AGENT-05"
  key_links:
    - from: "packages/agent_pipeline/pyproject.toml"
      to: "root pyproject.toml"
      via: "uv workspace members = packages/*"
      pattern: "packages/\\*"
    - from: "apps/bot/pyproject.toml"
      to: "packages/agent_pipeline"
      via: "sharpedge-agent-pipeline dependency"
      pattern: "sharpedge-agent-pipeline"
---

<objective>
Create the sharpedge-agent-pipeline package scaffold and all RED test stubs before any implementation begins.

Purpose: Wave 0 unblocks Waves 1–3 by (a) registering the new package in the uv workspace, (b) adding LangGraph + langchain-openai + tiktoken + langgraph-checkpoint-postgres as pinned dependencies, and (c) providing concrete failing tests that define the acceptance contract for every requirement ID.
Output: pyproject.toml for the new package, empty package __init__.py, 7 test stub files in RED state, apps/bot updated to depend on the new package.
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/revph/sharpedge/.planning/PROJECT.md
@/Users/revph/sharpedge/.planning/ROADMAP.md
@/Users/revph/sharpedge/.planning/STATE.md
@/Users/revph/sharpedge/.planning/phases/02-agent-architecture/02-RESEARCH.md
@/Users/revph/sharpedge/.planning/phases/02-agent-architecture/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent_pipeline package + register dependencies</name>
  <files>
    packages/agent_pipeline/pyproject.toml,
    packages/agent_pipeline/src/sharpedge_agent_pipeline/__init__.py,
    apps/bot/pyproject.toml
  </files>
  <behavior>
    - packages/agent_pipeline/pyproject.toml declares name="sharpedge-agent-pipeline", version="0.1.0", requires-python=">=3.12", with dependencies: langgraph>=1.1.0,&lt;2, langchain-openai>=0.3.0,&lt;0.4, langgraph-checkpoint-postgres>=3.0.0,&lt;4, tiktoken>=0.12.0,&lt;1, sharpedge-models, sharpedge-analytics, sharpedge-db, sharpedge-odds, sharpedge-shared
    - [tool.uv.sources] block maps each workspace package to workspace=true
    - packages/agent_pipeline/src/sharpedge_agent_pipeline/__init__.py contains only a docstring: "SharpEdge agent pipeline — LangGraph StateGraph orchestration."
    - apps/bot/pyproject.toml gains "sharpedge-agent-pipeline" in its dependencies list and "sharpedge-agent-pipeline = { workspace = true }" in [tool.uv.sources]
    - Root pyproject.toml MUST NOT be modified — [tool.uv.workspace] members = "packages/*" already covers agent_pipeline
    - Run `uv sync` after writing pyproject files to verify workspace resolution
  </behavior>
  <action>
    1. Create directory packages/agent_pipeline/src/sharpedge_agent_pipeline/ (requires __init__.py to be a package).
    2. Write packages/agent_pipeline/pyproject.toml with the exact dependency spec above. Use [tool.uv.sources] identical in structure to apps/bot/pyproject.toml's existing [tool.uv.sources] block.
    3. Write the __init__.py stub (docstring only — no imports yet, nothing will resolve until Wave 1 creates the modules).
    4. Edit apps/bot/pyproject.toml: append "sharpedge-agent-pipeline" to the dependencies list and add "sharpedge-agent-pipeline = { workspace = true }" under [tool.uv.sources].
    5. Run: uv sync --no-install-project (verify no dependency resolution errors).
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run python -c "import importlib.util; print(importlib.util.find_spec('sharpedge_agent_pipeline'))"</automated>
  </verify>
  <done>uv can locate sharpedge_agent_pipeline package; pyproject.toml contains all required deps; apps/bot depends on it</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for all 5 requirements</name>
  <files>
    tests/unit/agent_pipeline/__init__.py,
    tests/unit/agent_pipeline/test_graph.py,
    tests/unit/agent_pipeline/test_state.py,
    tests/unit/agent_pipeline/test_validate_setup.py,
    tests/unit/agent_pipeline/test_copilot.py,
    tests/unit/agent_pipeline/test_copilot_tools.py,
    tests/unit/agent_pipeline/test_session.py,
    tests/unit/agent_pipeline/test_alpha_ranker.py
  </files>
  <behavior>
    Each stub file contains:
    - A module docstring naming the requirement IDs it covers
    - Import of the not-yet-implemented module (will fail with ImportError — that IS the RED state)
    - At least one test function per VALIDATION.md row that uses pytest.mark.xfail(strict=True, reason="Wave 1 not yet implemented")

    test_graph.py covers (AGENT-01 + AGENT-02):
    - test_graph_executes_all_9_nodes: xfail — imports build_analysis_graph, invokes with mock state, asserts all node names visited
    - test_parallel_writes_no_collision: xfail — two parallel nodes each write distinct keys, both survive in result state
    - test_warn_retry_cap: xfail — WARN verdict with retry_count=2 routes to compose_alpha, not fetch_context

    test_state.py covers (AGENT-01):
    - test_quality_warnings_accumulate: xfail — Annotated[list[str], operator.add] merges two lists
    - test_parallel_keys_distinct: xfail — regime_result, ev_result, mc_result are separate TypedDict fields

    test_validate_setup.py covers (AGENT-02):
    - test_pass_verdict_returned: xfail — mock LLM returns PASS, node returns eval_verdict="PASS"
    - test_reject_verdict_returned: xfail — mock LLM returns REJECT
    - test_warn_increments_retry_count: xfail — WARN verdict increments retry_count

    test_copilot.py covers (AGENT-03):
    - test_active_bets_awareness: xfail — copilot response string contains bet game names from mocked get_active_bets

    test_copilot_tools.py covers (AGENT-04):
    - test_all_10_tools_return_valid_json: xfail — parametrized over 10 tool names, each returns parseable JSON

    test_session.py covers (AGENT-04):
    - test_trim_conversation_under_limit: xfail — short messages not trimmed
    - test_trim_conversation_over_limit: xfail — 200 messages trimmed to stay under MAX_TOKENS

    test_alpha_ranker.py covers (AGENT-05):
    - test_rank_by_alpha_descending: xfail — list sorted highest alpha first
    - test_none_alpha_last: xfail — plays with alpha_score=None sort after plays with alpha_score > 0
  </behavior>
  <action>
    Write each file as described. Use pytest.mark.xfail(strict=True) so the stubs ARE RED (not silently passing). Each file must have at least the imports that WILL be needed so the import error is triggered:
    - test_graph.py: `from sharpedge_agent_pipeline.graph import build_analysis_graph`
    - test_state.py: `from sharpedge_agent_pipeline.state import BettingAnalysisState`
    - test_validate_setup.py: `from sharpedge_agent_pipeline.nodes.validate_setup import validate_setup, SetupEvalResult`
    - test_copilot.py: `from sharpedge_agent_pipeline.copilot.agent import build_copilot_graph`
    - test_copilot_tools.py: `from sharpedge_agent_pipeline.copilot.tools import get_active_bets, get_portfolio_stats, analyze_game, search_value_plays, check_line_movement, get_sharp_indicators, estimate_bankroll_risk, get_prediction_market_edge, compare_books, get_model_predictions`
    - test_session.py: `from sharpedge_agent_pipeline.copilot.session import trim_conversation, MAX_TOKENS`
    - test_alpha_ranker.py: `from sharpedge_agent_pipeline.alerts.alpha_ranker import rank_by_alpha`
    Do NOT use try/except around imports — let them fail at collection time.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run pytest tests/unit/agent_pipeline/ -x -q 2>&1 | grep -E "(ERROR|FAILED|error)" | head -5</automated>
  </verify>
  <done>pytest exits non-zero (RED); at minimum ImportError on each stub file confirms Wave 1+ must create the modules</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv run python -c "import importlib.util; print(importlib.util.find_spec('sharpedge_agent_pipeline'))"` prints a ModuleSpec (not None)
2. `uv run pytest tests/unit/agent_pipeline/ -x -q` exits with non-zero code (RED — stubs fail)
3. `cat packages/agent_pipeline/pyproject.toml | grep langgraph` shows the version constraint
4. `grep sharpedge-agent-pipeline apps/bot/pyproject.toml` returns a match
</verification>

<success_criteria>
- Package directory packages/agent_pipeline/ exists with valid pyproject.toml
- uv workspace resolves sharpedge-agent-pipeline without errors
- 7 test stub files exist, each with failing tests in RED state
- apps/bot/pyproject.toml depends on sharpedge-agent-pipeline
- No implementation code exists yet (all modules are stubs or absent)
</success_criteria>

<output>
After completion, create `/Users/revph/sharpedge/.planning/phases/02-agent-architecture/02-01-SUMMARY.md`
</output>
