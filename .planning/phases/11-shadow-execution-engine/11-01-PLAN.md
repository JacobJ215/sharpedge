---
phase: 11-shadow-execution-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/venue_adapters/tests/test_shadow_execution_engine.py
  - packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-04

must_haves:
  truths:
    - "All 10 test stubs in test_shadow_execution_engine.py fail with ImportError or NotImplementedError, not with syntax errors"
    - "execution_engine.py exists with exported class skeletons so import succeeds after Wave 1 implementation"
    - "Test names exactly match the automated commands in the validation map"
  artifacts:
    - path: "packages/venue_adapters/tests/test_shadow_execution_engine.py"
      provides: "10 failing test stubs covering EXEC-01, EXEC-02, EXEC-04"
      min_lines: 80
    - path: "packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py"
      provides: "Stub module — OrderIntent, ShadowLedgerEntry, ShadowLedger, MarketExposureGuard, DayExposureGuard, ShadowExecutionEngine all raise NotImplementedError"
      min_lines: 60
  key_links:
    - from: "packages/venue_adapters/tests/test_shadow_execution_engine.py"
      to: "packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py"
      via: "from sharpedge_venue_adapters.execution_engine import ..."
      pattern: "from sharpedge_venue_adapters\\.execution_engine import"
---

<objective>
Write all failing test stubs for Phase 11 before any implementation exists.

Purpose: Nyquist rule compliance — every executable plan must have a Wave 0 test stub plan. These stubs define the contract that Plan 02 implements. CI will fail loudly until Plan 02 turns them green.
Output: test_shadow_execution_engine.py (10 stubs, all RED) + execution_engine.py (stub module so import resolves but methods raise NotImplementedError).
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-shadow-execution-engine/11-RESEARCH.md
@.planning/phases/11-shadow-execution-engine/11-VALIDATION.md

<interfaces>
<!-- Existing patterns the stubs must match. Extracted from codebase. -->

From packages/venue_adapters/src/sharpedge_venue_adapters/ledger.py:
```python
# LedgerEntry is frozen=True with UTC enforcement in __post_init__
@dataclass(frozen=True)
class LedgerEntry:
    entry_id: int | None
    ...
    def __post_init__(self) -> None:
        if self.timestamp_utc.tzinfo is None:
            raise ValueError("LedgerEntry.timestamp_utc must be UTC-aware")
```

From packages/venue_adapters/tests/conftest.py:
```python
@pytest.fixture
def utc_now():
    return datetime.now(timezone.utc)
```

New symbols to stub in execution_engine.py:
- OrderIntent (dataclass, frozen=True): market_id, predicted_edge, fair_prob, kelly_fraction, bankroll, created_at
- ShadowLedgerEntry (dataclass, frozen=True): entry_id, market_id, predicted_edge, kelly_sized_amount, timestamp
- ShadowLedger: __init__, append(entry) -> ShadowLedgerEntry, entries property (list)
- MarketExposureGuard: __init__(max_market_exposure), would_breach(market_id, stake), commit(market_id, stake)
- DayExposureGuard: __init__(max_day_exposure), would_breach(stake), commit(stake), day_stake property
- ShadowExecutionEngine: __init__(max_market_exposure, max_day_exposure), process_intent(intent) -> ShadowLedgerEntry | None, shadow_ledger property
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write execution_engine.py stub module</name>
  <files>packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py</files>
  <behavior>
    - All class constructors (__init__) must be callable without raising
    - All methods except __init__ and simple property getters raise NotImplementedError
    - ShadowLedgerEntry.__post_init__ raises ValueError on naive timestamp (this IS implemented in the stub — it's a contract, not logic)
    - Module is importable: `from sharpedge_venue_adapters.execution_engine import ShadowExecutionEngine` succeeds
  </behavior>
  <action>
Create `packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py` with the following stubs:

1. `OrderIntent` — frozen dataclass with fields: market_id: str, predicted_edge: float, fair_prob: float, kelly_fraction: float, bankroll: float, created_at: datetime. Add `__post_init__` raising ValueError if created_at.tzinfo is None.

2. `ShadowLedgerEntry` — frozen dataclass with fields: entry_id: int | None, market_id: str, predicted_edge: float, kelly_sized_amount: float, timestamp: datetime. Add `__post_init__` raising ValueError if timestamp.tzinfo is None.

3. `ShadowLedger` — class with `__init__(self)` setting `self._entries: list[ShadowLedgerEntry] = []`. Method `append(self, entry: ShadowLedgerEntry) -> ShadowLedgerEntry` raises NotImplementedError. Property `entries` returns `list(self._entries)`.

4. `MarketExposureGuard` — class with `__init__(self, max_market_exposure: float)`. Methods `would_breach(self, market_id: str, stake: float) -> bool` and `commit(self, market_id: str, stake: float) -> None` both raise NotImplementedError.

5. `DayExposureGuard` — class with `__init__(self, max_day_exposure: float)`. Methods `would_breach(self, stake: float) -> bool` and `commit(self, stake: float) -> None` raise NotImplementedError. Property `day_stake` raises NotImplementedError.

6. `ShadowExecutionEngine` — class with `__init__(self, max_market_exposure: float, max_day_exposure: float)` that instantiates `MarketExposureGuard`, `DayExposureGuard`, and `ShadowLedger` as instance attributes. Method `process_intent(self, intent: OrderIntent) -> ShadowLedgerEntry | None` raises NotImplementedError. Property `shadow_ledger` returns `self._ledger`.

Add module-level `logger = logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && python -c "from sharpedge_venue_adapters.execution_engine import ShadowExecutionEngine, ShadowLedgerEntry, OrderIntent, ShadowLedger, MarketExposureGuard, DayExposureGuard; print('import ok')"</automated>
  </verify>
  <done>Module importable. All 6 classes present. ShadowLedgerEntry and OrderIntent raise ValueError on naive timestamp. Other methods raise NotImplementedError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test stubs (RED)</name>
  <files>packages/venue_adapters/tests/test_shadow_execution_engine.py</files>
  <behavior>
    - test_shadow_mode_no_kalshi_calls: asserts process_intent raises NotImplementedError (stub behavior) — will pass RED phase
    - test_shadow_mode_detection: asserts ShadowExecutionEngine instantiates without ENABLE_KALSHI_EXECUTION set
    - test_ledger_entry_fields: asserts ShadowLedgerEntry has market_id, predicted_edge, kelly_sized_amount, timestamp attributes
    - test_naive_timestamp_rejected: asserts ShadowLedgerEntry raises ValueError when timestamp has no tzinfo
    - test_per_market_limit_rejection: asserts process_intent returns None when per-market limit would be breached (raises NotImplementedError in stub)
    - test_per_market_rejection_no_ledger_write: asserts len(engine.shadow_ledger.entries) == 0 after rejection
    - test_per_day_limit_rejection: asserts process_intent returns None when day limit would be breached (raises NotImplementedError in stub)
    - test_per_day_rejection_no_ledger_write: asserts len(engine.shadow_ledger.entries) == 0 after day-limit rejection
    - test_per_market_cap_boundary: asserts intents up to cap are accepted; intent that exceeds is not
    - test_day_stake_resets_at_midnight: asserts DayExposureGuard resets stake when UTC date changes
  </behavior>
  <action>
Create `packages/venue_adapters/tests/test_shadow_execution_engine.py`.

**Import block:**
```python
import os
import pytest
from datetime import datetime, timezone, timedelta
from unittest.mock import patch
from sharpedge_venue_adapters.execution_engine import (
    OrderIntent,
    ShadowLedgerEntry,
    ShadowLedger,
    MarketExposureGuard,
    DayExposureGuard,
    ShadowExecutionEngine,
)
```

**Helper fixture** — `make_intent(market_id, kelly_fraction, bankroll)` that returns an OrderIntent with UTC timestamp and plausible defaults (predicted_edge=0.05, fair_prob=0.58).

**Write 10 test functions** using exact names from the validation map:

1. `test_shadow_mode_no_kalshi_calls` — instantiate engine, call process_intent, verify `os.environ` does not contain `ENABLE_KALSHI_EXECUTION` during test (use `monkeypatch.delenv`). For RED phase: wrap call in `pytest.raises(NotImplementedError)` because stub raises it. After Wave 1 the test is updated to assert `result` is a `ShadowLedgerEntry`.

   IMPORTANT: Write the test in its FINAL (GREEN) form — asserting the real behavior — not the interim NotImplementedError form. The test will fail (RED) because the method raises NotImplementedError, but the test body should assert what Phase 11 success looks like:
   ```python
   result = engine.process_intent(intent)
   assert isinstance(result, ShadowLedgerEntry)
   assert result.market_id == intent.market_id
   ```

2. `test_shadow_mode_detection` — check that `ShadowExecutionEngine(max_market_exposure=500.0, max_day_exposure=2000.0)` instantiates without error when `ENABLE_KALSHI_EXECUTION` is absent from env.

3. `test_ledger_entry_fields` — construct a `ShadowLedgerEntry` with valid UTC timestamp and assert `.market_id`, `.predicted_edge`, `.kelly_sized_amount`, `.timestamp` attributes are accessible with correct values.

4. `test_naive_timestamp_rejected` — construct a `ShadowLedgerEntry` with `timestamp=datetime(2026, 3, 17, 12, 0, 0)` (no tzinfo) and assert `ValueError` is raised.

5. `test_per_market_limit_rejection` — create engine with `max_market_exposure=500.0`. Submit first intent committing $400 to "MKTX". Submit second intent that would commit another $400 to same market. Assert second call returns `None`.

6. `test_per_market_rejection_no_ledger_write` — same setup as test 5. After the second (rejected) call, assert `len(engine.shadow_ledger.entries) == 1` (only the first accepted intent).

7. `test_per_day_limit_rejection` — create engine with `max_day_exposure=800.0`. Submit intents that cumulatively exceed $800. Assert the over-limit intent returns `None`.

8. `test_per_day_rejection_no_ledger_write` — same setup as test 7. Assert ledger entry count does not increase after the rejected intent.

9. `test_per_market_cap_boundary` — engine with `max_market_exposure=500.0`. Submit intent committing exactly $500. Assert it is accepted. Submit a $1 intent for same market. Assert it returns `None`.

10. `test_day_stake_resets_at_midnight` — instantiate `DayExposureGuard(max_day_exposure=1000.0)`. Call `commit(900.0)`. Verify `would_breach(200.0)` returns True (over limit). Mock `datetime.now` in `sharpedge_venue_adapters.execution_engine` to return a date one day later. Verify `would_breach(200.0)` returns False (reset happened).

All 10 tests should fail because stub methods raise NotImplementedError (or assertion failures on returning None vs ShadowLedgerEntry). This is the correct RED state.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run pytest packages/venue_adapters/tests/test_shadow_execution_engine.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>File exists. All 10 test functions present with exact names matching the validation map. Running pytest shows failures (ImportError-free, all fail on assertion or NotImplementedError). Zero syntax errors — `python -m py_compile packages/venue_adapters/tests/test_shadow_execution_engine.py` exits 0.</done>
</task>

</tasks>

<verification>
After both tasks:
- `python -m py_compile packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py` — exit 0
- `python -m py_compile packages/venue_adapters/tests/test_shadow_execution_engine.py` — exit 0
- `uv run pytest packages/venue_adapters/tests/test_shadow_execution_engine.py -q 2>&1 | grep -E "FAILED|ERROR|passed"` — shows FAILED (not ERROR, not 0 passed)
- All 10 test names are present: `grep "^def test_" packages/venue_adapters/tests/test_shadow_execution_engine.py | wc -l` returns 10
</verification>

<success_criteria>
- execution_engine.py importable, 6 classes exported, stubs raise NotImplementedError (except __post_init__ UTC checks which are fully implemented)
- test_shadow_execution_engine.py has exactly 10 test functions matching the VALIDATION.md names
- All 10 tests FAIL (RED) — zero syntax errors, no ImportError, only assertion/NotImplementedError failures
- CI will fail loudly until Plan 02 turns these green
</success_criteria>

<output>
After completion, create `.planning/phases/11-shadow-execution-engine/11-01-SUMMARY.md`
</output>
