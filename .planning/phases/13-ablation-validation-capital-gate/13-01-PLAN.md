---
phase: 13-ablation-validation-capital-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py
  - packages/venue_adapters/tests/test_capital_gate.py
  - packages/venue_adapters/tests/test_ablation.py
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - ABLATE-01
  - ABLATE-02

must_haves:
  truths:
    - "All GATE and ABLATE test files exist and import without ImportError"
    - "All tests FAIL with NotImplementedError or AssertionError (RED phase)"
    - "capital_gate.py stub exports CapitalGate, CapitalGateError, GateStatus, GateCondition"
  artifacts:
    - path: "packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py"
      provides: "CapitalGate stub class with NotImplementedError bodies"
      exports: ["CapitalGate", "CapitalGateError", "GateStatus", "GateCondition"]
    - path: "packages/venue_adapters/tests/test_capital_gate.py"
      provides: "RED test stubs for GATE-01 through GATE-04 + assert_ready()"
      min_lines: 100
    - path: "packages/venue_adapters/tests/test_ablation.py"
      provides: "RED test stubs for ABLATE-01 and ABLATE-02"
      min_lines: 40
  key_links:
    - from: "packages/venue_adapters/tests/test_capital_gate.py"
      to: "packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py"
      via: "import CapitalGate, CapitalGateError, GateStatus, GateCondition"
      pattern: "from sharpedge_venue_adapters\\.capital_gate import"
---

<objective>
Create Wave 0 RED test stubs and stub module for the CapitalGate class and ablation logic. All tests must fail with NotImplementedError (not ImportError), confirming the interface contracts are locked before Plan 02 implements them.

Purpose: TDD Wave 0 — lock interface contracts so Plan 02 implementation requires zero test file changes.
Output: `capital_gate.py` stub, `test_capital_gate.py` (16 RED tests), `test_ablation.py` (3 RED tests)
</objective>

<execution_context>
@/Users/revph/.claude/get-shit-done/workflows/execute-plan.md
@/Users/revph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ablation-validation-capital-gate/13-CONTEXT.md
@.planning/phases/13-ablation-validation-capital-gate/13-RESEARCH.md
@.planning/phases/13-ablation-validation-capital-gate/13-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor needs -->

From packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py:
```python
@dataclass(frozen=True)
class ShadowLedgerEntry:
    entry_id: Optional[int]
    market_id: str
    predicted_edge: float       # fraction
    kelly_sized_amount: float   # USD
    timestamp: datetime         # UTC-aware
    position_lot_id: str = ""

class ShadowLedger:
    def append(self, entry: ShadowLedgerEntry) -> ShadowLedgerEntry: ...
    @property
    def entries(self) -> list[ShadowLedgerEntry]: ...

class ShadowExecutionEngine:
    @classmethod
    def from_env(cls) -> "ShadowExecutionEngine": ...
    async def process_intent(self, intent: OrderIntent) -> Optional[ShadowLedgerEntry]: ...
```

From packages/trading_swarm/src/sharpedge_trading/agents/risk_agent.py:
```python
@dataclass
class CircuitBreakerState:
    consecutive_losses: int = 0
    daily_loss: float = 0.0
    daily_loss_reset_date: str = ""  # YYYY-MM-DD UTC
    paused_until: datetime | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create capital_gate.py stub + test_capital_gate.py RED tests</name>
  <files>
    packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py
    packages/venue_adapters/tests/test_capital_gate.py
  </files>
  <read_first>
    - packages/venue_adapters/src/sharpedge_venue_adapters/execution_engine.py (ShadowLedgerEntry, ShadowLedger, from_env() injection point)
    - packages/trading_swarm/src/sharpedge_trading/agents/risk_agent.py (CircuitBreakerState daily reset pattern)
    - packages/venue_adapters/tests/test_shadow_execution_engine.py (test pattern reference)
    - packages/venue_adapters/pyproject.toml (asyncio_mode = "auto", testpaths)
    - .planning/phases/13-ablation-validation-capital-gate/13-RESEARCH.md (Pattern 1-5, test map)
  </read_first>
  <behavior>
    - test_gate01_fails_missing_artifact: Create CapitalGate with tmp_path models_dir missing one .joblib → check() returns GateCondition(name="GATE-01", passed=False)
    - test_gate01_passes_all_artifacts: Create all 5 .joblib files in tmp_path → check() returns GateCondition(name="GATE-01", passed=True)
    - test_gate02_fails_insufficient_days: Mock Supabase returning <7 days of shadow_ledger rows → check() returns GATE-02 failed
    - test_gate02_fails_low_positive_rate: Mock Supabase returning 7+ days but positive_signal_rate <55% → GATE-02 failed
    - test_gate02_fails_low_mean_edge: Mock Supabase returning 7+ days, rate >=55%, but mean_edge <1.5% → GATE-02 failed
    - test_gate02_passes_valid_period: Mock Supabase returning 7+ days, rate >=55%, mean_edge >=1.5% → GATE-02 passed
    - test_gate03_fails_no_approval_file: No live_approval.json → GATE-03 failed
    - test_gate03_fails_invalid_json: Write garbage to live_approval.json → GATE-03 failed
    - test_gate03_fails_stale_approval: Write approval with gate_snapshot.gate_01_models=False → GATE-03 failed (stale)
    - test_gate03_passes_valid_approval: Write valid live_approval.json with all snapshot fields True → GATE-03 passed
    - test_gate04_no_breach_below_threshold: record_daily_loss(10.0, 10000.0) with 10% threshold → returns False
    - test_gate04_breach_invalidates_approval: record_daily_loss(1100.0, 10000.0) with 10% threshold → returns True, live_approval.json renamed to .disabled
    - test_gate04_daily_reset: Record loss day 1, patch datetime to day 2 → daily_loss resets to 0
    - test_gate04_check_fails_after_breach: After breach (approval renamed to .disabled) → check() GATE-03 fails
    - test_assert_ready_collects_all_failures: All 4 gates failing → CapitalGateError message contains all 4 reasons
    - test_assert_ready_raises_capital_gate_error: assert_ready() raises CapitalGateError (not generic RuntimeError)
  </behavior>
  <action>
    **Step 1: Create `capital_gate.py` stub** at `packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py`:

    Define these exports with `NotImplementedError` bodies:
    - `CapitalGateError(RuntimeError)` — custom exception class (fully implemented, no stub needed)
    - `GateCondition` dataclass: `name: str`, `passed: bool`, `reason: str`
    - `GateStatus` dataclass: `conditions: list[GateCondition]`, property `all_passed -> bool` (returns `all(c.passed for c in self.conditions)`), property `failures -> list[GateCondition]`
    - `CapitalGate` class:
      - `__init__(self, models_dir: Path, approval_path: Path, min_paper_days: int = 7, min_positive_rate: float = 0.55, min_mean_edge: float = 0.015, daily_loss_pct: float = 0.10)` — store params only, body is real (not stub)
      - `check(self) -> GateStatus` — `raise NotImplementedError`
      - `assert_ready(self) -> None` — `raise NotImplementedError`
      - `record_daily_loss(self, amount_usd: float, bankroll: float) -> bool` — `raise NotImplementedError`
      - `@classmethod from_env(cls) -> CapitalGate` — `raise NotImplementedError`

    Constants at module level:
    - `CATEGORIES = ("crypto", "economic", "entertainment", "political", "weather")`
    - `_DEFAULT_MODELS_DIR = Path("data/models/pm")`
    - `_DEFAULT_APPROVAL_PATH = Path("data/live_approval.json")`

    **Step 2: Create `test_capital_gate.py`** at `packages/venue_adapters/tests/test_capital_gate.py`:

    Import: `from sharpedge_venue_adapters.capital_gate import CapitalGate, CapitalGateError, GateStatus, GateCondition, CATEGORIES`

    Create a `@pytest.fixture` named `make_gate` that accepts `tmp_path` and returns a factory function creating `CapitalGate(models_dir=tmp_path / "models", approval_path=tmp_path / "live_approval.json")`.

    Create a `@pytest.fixture` named `populated_models` that writes 5 empty `.joblib` files to `tmp_path / "models" / "{cat}.joblib"` for each category.

    Write all 16 tests listed in <behavior>. Each test calls the stub method, which raises NotImplementedError — making the test RED. Tests are written in GREEN assertion form so Plan 02 implementation requires zero test modifications.

    For GATE-02 tests: mock `supabase.create_client` and the `.table("shadow_ledger").select("*").gte().execute()` chain. Use `unittest.mock.patch` targeting `sharpedge_venue_adapters.capital_gate.create_client`.

    For GATE-04 datetime patching: patch `sharpedge_venue_adapters.capital_gate.datetime` the same way Phase 11 tests patch `sharpedge_venue_adapters.execution_engine.datetime`.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run --package sharpedge-venue-adapters pytest packages/venue_adapters/tests/test_capital_gate.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" packages/venue_adapters/tests/test_capital_gate.py` returns 16
    - `grep "class CapitalGate" packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py` matches
    - `grep "class CapitalGateError" packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py` matches
    - `grep "class GateCondition" packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py` matches
    - `grep "class GateStatus" packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py` matches
    - `grep "NotImplementedError" packages/venue_adapters/src/sharpedge_venue_adapters/capital_gate.py` returns at least 3 lines
    - Running pytest produces 16 FAILED, 0 PASSED, 0 ERRORS (all failures are NotImplementedError, not ImportError)
  </acceptance_criteria>
  <done>16 RED tests exist in test_capital_gate.py. capital_gate.py stub exports all types. All tests fail with NotImplementedError, not ImportError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_ablation.py RED tests</name>
  <files>
    packages/venue_adapters/tests/test_ablation.py
  </files>
  <read_first>
    - .planning/phases/13-ablation-validation-capital-gate/13-RESEARCH.md (Pattern 4: ablation report schema, ablation test map)
    - packages/venue_adapters/tests/test_capital_gate.py (just-created test pattern)
  </read_first>
  <behavior>
    - test_ablation_computes_per_category_delta: Given mock resolved markets with known model_prob and market_price, compute_ablation_report() returns per-category delta = model_edge - fallback_edge
    - test_ablation_pass_threshold: Given all category deltas >= 0.0% and overall delta >= 1.5% → report.passed = True
    - test_ablation_fail_negative_category: Given one category with negative delta → report.passed = False, that category shows passed=False
  </behavior>
  <action>
    **Create `test_ablation.py`** at `packages/venue_adapters/tests/test_ablation.py`.

    The ablation logic will live as a helper function `compute_ablation_report(resolved_markets: list[dict], models_dir: Path, fee_rate: float = 0.05, threshold_pct: float = 1.5) -> dict` inside a new module. For Wave 0, define this function as a stub in `capital_gate.py` (append to existing file) OR in a new `packages/venue_adapters/src/sharpedge_venue_adapters/ablation.py` file.

    Decision: Create `packages/venue_adapters/src/sharpedge_venue_adapters/ablation.py` with:
    - `compute_ablation_report(resolved_markets: list[dict], models_dir: Path, fee_rate: float = 0.05, threshold_pct: float = 1.5) -> dict` — `raise NotImplementedError`

    The test file imports `from sharpedge_venue_adapters.ablation import compute_ablation_report`.

    Each test provides mock resolved_markets data as a list of dicts with keys: `category`, `market_price`, `resolved_yes` (0 or 1). The tests call `compute_ablation_report(...)` which raises NotImplementedError — making them RED.

    Tests are written in GREEN assertion form:
    - `test_ablation_computes_per_category_delta`: Assert `result["categories"]["crypto"]["delta"]` is approximately `model_edge - fallback_edge`
    - `test_ablation_pass_threshold`: Assert `result["passed"] is True` and all categories have `"passed": True`
    - `test_ablation_fail_negative_category`: Assert `result["passed"] is False` and the bad category has `"passed": False`

    Also create the stub file `packages/venue_adapters/src/sharpedge_venue_adapters/ablation.py`.
  </action>
  <verify>
    <automated>cd /Users/revph/sharpedge && uv run --package sharpedge-venue-adapters pytest packages/venue_adapters/tests/test_ablation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" packages/venue_adapters/tests/test_ablation.py` returns 3
    - `grep "compute_ablation_report" packages/venue_adapters/src/sharpedge_venue_adapters/ablation.py` matches
    - `grep "NotImplementedError" packages/venue_adapters/src/sharpedge_venue_adapters/ablation.py` matches
    - Running pytest produces 3 FAILED, 0 PASSED, 0 ERRORS (NotImplementedError failures only)
  </acceptance_criteria>
  <done>3 RED tests exist in test_ablation.py. ablation.py stub exports compute_ablation_report. All tests fail with NotImplementedError.</done>
</task>

</tasks>

<verification>
All 19 tests (16 capital_gate + 3 ablation) fail with NotImplementedError. Zero ImportErrors. Zero syntax errors.

```bash
uv run --package sharpedge-venue-adapters pytest packages/venue_adapters/tests/test_capital_gate.py packages/venue_adapters/tests/test_ablation.py -q
```

Expected: 19 failed, 0 passed, 0 errors.
</verification>

<success_criteria>
- 19 RED tests exist across two test files
- capital_gate.py stub exports: CapitalGate, CapitalGateError, GateStatus, GateCondition, CATEGORIES
- ablation.py stub exports: compute_ablation_report
- All stub methods raise NotImplementedError
- No ImportErrors — all imports resolve cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/13-ablation-validation-capital-gate/13-01-SUMMARY.md`
</output>
