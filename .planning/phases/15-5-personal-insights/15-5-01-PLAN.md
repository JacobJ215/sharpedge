# 15.5 Plan 01 — POST /api/v1/bets + portfolio history fixes

## Scope

1. **`POST /api/v1/bets`** — Auth required; body matches mobile `logBet` (`play_id`, `event`, `market`, `team`, `book`, `stake`). Resolve Supabase user → `public.users.id`; load play via `get_value_play`; insert via `create_bet` with units derived from `users.unit_size` (fallback `$50` unit like `bet_service`).
2. **`get_user_bets_history`** — Include **pending** bets; order by `created_at` desc; select `sportsbook`, `clv_points`; expose `clv` alias for portfolio.
3. **`portfolio` route** — Treat pending case-insensitively; use `clv` from history dicts.

## Verification

- `uv run pytest apps/webhook_server/tests/unit/api/test_portfolio.py`
- `uv run pytest apps/webhook_server/tests/unit/api/test_bets_log.py` (new)

## Out of scope

- Discord `research.py` still passing discord snowflake as `user_id` into `get_user_bets_history` (pre-existing); fix in a Discord-focused pass if needed.
