# 15.5 Plan 02 — Portfolio breakdowns (API + compact web)

## Scope

1. Extend **`GET /api/v1/users/{id}/portfolio`** with settled-bet aggregates: sport, bet type, sportsbook, American odds / juice buckets, plus **`unit_size`** for display.
2. **Web**: one compact “Performance splits” block on the existing portfolio page (no new nav).

## Done criteria

- [x] Response includes `unit_size`, `by_sport`, `by_bet_type`, `by_book`, `by_juice`.
- [x] Portfolio page renders tables when arrays non-empty; copy when empty.
- [x] Unit tests mock new portfolio dependencies.

## References

- `packages/database/src/sharpedge_db/queries/bets.py` — `get_breakdown_by_sportsbook`, `get_breakdown_by_juice_bucket`, `american_odds_juice_bucket`
- `apps/webhook_server/.../routes/v1/portfolio.py` — `load_portfolio_breakdowns`
